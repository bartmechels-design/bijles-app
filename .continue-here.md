# Continue Here — Handoff 2026-03-07

## Where We Are

**Not in a GSD phase.** Working outside the roadmap on critical bug fixes
that came up after the production deployment (Phase 12 complete).

**Two active bugs being fixed:**

1. **TTS geeft 500** — OpenAI TTS route crasht op Vercel/lokaal. Root cause nog
   onbekend. OpenAI API key is geldig (200 OK bij directe test). Supabase auth
   werkt voor de chat route. Iets crasht specifiek in de TTS route.

2. **LaTeX breuken in OPDRACHT blokken** — grotendeels opgelost maar nog niet
   getest na de laatste fixes.

---

## Exact Stop Point

Bezig met debuggen van de TTS 500 error. Console toont:
```
Failed to load resource: /nl/api/tutor/tts:1 a status of 500 ()
```

Laatste actie vóór /compact:
- Debug logging toegevoegd aan route.ts (error message in response body)
- Debug logging toegevoegd aan useSpeech.ts (console.warn → moet console.error worden)
- Wilde `console.warn` veranderen naar `console.error` in `fetchChunk` in
  `aruba-leren/src/hooks/useSpeech.ts` (regel ~28)
- Die edit mislukte omdat het bestand niet gelezen was

---

## What Was Done This Session

### Commits (chronologisch):
- `8d7249d` fix: render LaTeX breuken als echte visuele breuken via KaTeX
- `78f3106` fix: no LaTeX in AI output, strip delimiters in display, TTS autoplay fallback
- `8077cb7` fix: Koko start lessen proactief, kladblaadje verwijderd, LaTeX TTS fix
- `502d523` feat: add handelingsmodel to Koko prompt
- `91aa84f` feat: fix TTS, language switching, rapport translations
- `e1bd1fd` fix: unlock AudioContext synchronously on user gesture
- `be18495` fix: pre-fetch TTS buffer for synchronous iOS gesture unlock
- `682eba9` fix: render LaTeX in text segments when message has special blocks
- `4c9cb73` fix: always unlock speechSynthesis synchronously on banner tap (iOS)
- `1cc809f` fix: renderMathLine handles bare \frac{}{} without \( delimiters
- `dc60a9d` fix: speechSynthesis fallback when OpenAI TTS buffer is null
- `d60b423` fix: strip \( \) delimiters before math-to-speech conversion in TTS
- `b395879` debug: log exact TTS API error reason to browser console

### Bugs opgelost:
- LaTeX breuken (visueel) in normale berichten ✅
- LaTeX breuken in OPDRACHT blokken ✅
- LaTeX breuken in tekst-segmenten naast [SPREEK]/[BORD] blokken ✅
- `renderMathLine` logicafout (stripped delimiters dan splitste erop) ✅
- `\frac` zonder `\(` delimiters in OPDRACHT blokken ✅
- TTS las `\frac{8}{12}` voor als tekst → nu "8 twaalfde" ✅
- Koko start les proactief via `[START_SESSIE]` ✅
- Kladblaadje verwijderd uit student UI ✅
- Audio-unlock banner voor browser autoplay policy ✅
- iOS speechSynthesis unlock patroon ✅
- speechSynthesis fallback als OpenAI TTS null teruggeeft ✅

### Nog niet opgelost:
- **OpenAI TTS geeft 500 op Vercel** — robot-stem (browser TTS) werkt als fallback,
  maar de OpenAI TTS (mooie stem) werkt niet

---

## TTS Debug State

### Vastgestelde feiten:
- OpenAI API key in `.env.local`: geldig, HTTP 200 op /v1/models
- OpenAI TTS direct (Node.js test): werkt, 27360 bytes audio ontvangen
- openai package v6.25.0: geinstalleerd, `arrayBuffer()` werkt correct
- Dev server: was NIET draaiend op moment van debug
- Vercel deployment: app is live op `bijles-app-4vip.vercel.app/nl`
- Console fout: `500 ()` van `/nl/api/tutor/tts`

### Mogelijke oorzaken (nog niet geverifieerd):
1. Supabase `createClient()` gooit exception ipv error returnen
   → wordt gecatch → 500 (i.p.v. 401)
2. OPENAI_API_KEY niet goed gezet op Vercel (maar is toegevoegd)
3. Import/module probleem in de TTS route op Vercel runtime
4. `mp3Response.arrayBuffer()` gooit op Vercel (andere runtime?)

### Volgende debug stap:
Het exacte foutbericht zien. De route retourneert nu `TTS error: ${msg}`.
`fetchChunk` logt nu `console.warn` (niet zichtbaar door filter).

**Eerst doen:** verander `console.warn` naar `console.error` in
`aruba-leren/src/hooks/useSpeech.ts` rond regel 28:
```typescript
// Oud:
console.warn(`TTS API ${response.status}:`, errText);
// Nieuw:
console.error(`[TTS] ${response.status}: ${errText}`);
```

Dan deployen en de gebruiker de console laten checken voor het rode bericht.

**Alternatief:** Network tab in Chrome DevTools → klik op het `/nl/api/tutor/tts`
verzoek → Response tab → zie de exacte foutmelding.

---

## Key Files

- TTS route: `aruba-leren/src/app/[locale]/api/tutor/tts/route.ts`
- TTS hook: `aruba-leren/src/hooks/useSpeech.ts`
- TTS utils: `aruba-leren/src/lib/ai/tts-utils.ts`
- Chat UI: `aruba-leren/src/components/tutor/ChatInterface.tsx`
- Chat message: `aruba-leren/src/components/tutor/ChatMessage.tsx`
- Supabase server client: `aruba-leren/src/lib/supabase/server.ts`

## Environment

- Vercel URL: `https://bijles-app-4vip.vercel.app/nl`
- OPENAI_API_KEY: aanwezig in .env.local EN toegevoegd aan Vercel
- Branch: `main` (alles gepusht)
