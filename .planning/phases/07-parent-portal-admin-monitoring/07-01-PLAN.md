---
phase: 07-parent-portal-admin-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - aruba-leren/src/app/[locale]/dashboard/page.tsx
  - aruba-leren/src/components/progress/ProgressSummaryCard.tsx
  - aruba-leren/src/app/[locale]/dashboard/kind/[childId]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Parent sees progress overview per child on the dashboard (level, subject, stuck indicator)"
    - "Parent can click through to a per-child detail page showing all subjects"
    - "Parent can access subscription status page via link on dashboard"
    - "Parent can still add and edit children from the dashboard"
  artifacts:
    - path: "aruba-leren/src/components/progress/ProgressSummaryCard.tsx"
      provides: "Compact progress card per child for dashboard"
      min_lines: 30
    - path: "aruba-leren/src/app/[locale]/dashboard/kind/[childId]/page.tsx"
      provides: "Per-child detail page with full subject progress"
      min_lines: 40
    - path: "aruba-leren/src/app/[locale]/dashboard/page.tsx"
      provides: "Enhanced parent dashboard with progress cards"
      contains: "ProgressSummaryCard"
  key_links:
    - from: "aruba-leren/src/app/[locale]/dashboard/page.tsx"
      to: "child_subject_progress"
      via: "Supabase .in('child_id', childIds) batch query"
      pattern: "\\.in\\('child_id'"
    - from: "aruba-leren/src/components/progress/ProgressSummaryCard.tsx"
      to: "aruba-leren/src/components/progress/SubjectProgress.tsx"
      via: "import and render"
      pattern: "import.*SubjectProgress"
    - from: "aruba-leren/src/app/[locale]/dashboard/kind/[childId]/page.tsx"
      to: "child_subject_progress"
      via: "Supabase query filtered by childId"
      pattern: "\\.eq\\('child_id'"
---

<objective>
Enhance the parent dashboard with progress visualization per child and a per-child detail page.

Purpose: Parents need full visibility into their children's learning progress (OUDER-01). The existing dashboard shows children but no progress data. This plan adds progress cards to the dashboard and a drill-down detail page per child. OUDER-02 (edit/add children) and OUDER-03 (subscription link) already exist — we verify they are accessible from the enhanced dashboard.

Output: Enhanced dashboard with ProgressSummaryCard components showing level + subject progress per child, plus a `/dashboard/kind/[childId]` detail page with full subject breakdown.
</objective>

<execution_context>
@C:/Users/Familie Mechels/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Familie Mechels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@aruba-leren/src/app/[locale]/dashboard/page.tsx
@aruba-leren/src/components/progress/SubjectProgress.tsx
@aruba-leren/src/components/progress/ProgressBar.tsx
@aruba-leren/src/components/progress/LevelBadge.tsx
@aruba-leren/src/types/progress.ts
@aruba-leren/src/types/tutoring.ts
@aruba-leren/src/components/ChildList.tsx
@aruba-leren/src/components/AddChildForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProgressSummaryCard and per-child detail page</name>
  <files>
    aruba-leren/src/components/progress/ProgressSummaryCard.tsx
    aruba-leren/src/app/[locale]/dashboard/kind/[childId]/page.tsx
  </files>
  <action>
Create two new files:

**ProgressSummaryCard.tsx** — A `'use client'` component that displays a compact progress overview for a single child on the parent dashboard. Props: `{ child: { id, first_name, grade }, progress: Record<string, ChildSubjectProgress>, locale: string }`.

Layout:
- Card with `bg-white rounded-2xl shadow-lg p-6 border-2 border-sky-100`
- Header row: child name (text-xl font-bold text-sky-700) + "Klas {grade}" subtitle + "Details >" link to `/${locale}/dashboard/kind/${child.id}`
- For each of the 6 SUBJECTS (import from `@/types/tutoring`), render the subject label (labelNl) and reuse the existing `SubjectProgress` component (import from `./SubjectProgress`), passing `progress[subject.id] ?? null`
- If no progress entries exist at all, show italic gray message "Nog geen lessen gevolgd"
- Mobile-first: full width on mobile, cards stack vertically

**Per-child detail page** (`kind/[childId]/page.tsx`) — A Server Component page showing full progress for one child.
- Authenticate user, fetch profile via `.eq('user_id', user.id)`, get child via `.eq('id', childId).eq('parent_id', profile.id).single()` — CRITICAL: use `profile.id` not `user.id` for parent_id
- If child not found or not owned by this parent, redirect to dashboard
- Fetch all `child_subject_progress` rows for this child via `.eq('child_id', childId)`
- Fetch recent `progress_events` for this child (last 10) via `.eq('child_id', childId).order('created_at', { ascending: false }).limit(10)`
- Render page with:
  - Back link to `/${locale}/dashboard`
  - Child name + grade header
  - Grid of all 6 subjects, each with SubjectProgress component showing level, progress bar, stuck indicator
  - For each subject with assessment_completed: show total_sessions count and last_session_at date
  - Recent activity section: list of progress_events showing event_type + subject + created_at formatted as relative time or date
- Mobile-first grid: 1 col on mobile, 2 cols on md+
  </action>
  <verify>
Run `npx tsc --noEmit` from aruba-leren/ — no type errors in new files. Check that both files import from existing modules correctly.
  </verify>
  <done>
ProgressSummaryCard renders child name, grade, and SubjectProgress for all 6 subjects. Per-child detail page loads progress data with proper parent_id ownership check and shows full progress breakdown with recent activity.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance parent dashboard with progress data and cards</name>
  <files>
    aruba-leren/src/app/[locale]/dashboard/page.tsx
  </files>
  <action>
Modify the existing dashboard page to add progress visualization. The existing page already fetches `profile`, `user`, `children`, and `subscription`. Extend it:

**Data fetching (after children fetch, before render):**
- Extract `childIds` from children array: `const childIds = (children || []).map(c => c.id)`
- If childIds is non-empty, batch-fetch progress: `const { data: progressRows } = await supabase.from('child_subject_progress').select('child_id, subject, current_level, assessment_completed, is_stuck, stuck_since, last_session_at, total_sessions, total_correct, total_incorrect, total_hints_received').in('child_id', childIds)`
- Build progressByChild map: `Record<string, Record<string, ChildSubjectProgress>>` — iterate progressRows, group by child_id then subject
- If childIds is empty, set progressByChild to empty object

**Render changes (within the hasActiveSubscription branch):**
- KEEP the existing "Bijles met Koko" CTA card at the top
- KEEP the existing AddChildForm and ChildList section
- ADD a new section BETWEEN the Koko CTA and the form/list section: "Voortgang" header (text-2xl font-bold) followed by a `grid grid-cols-1 md:grid-cols-2 gap-6` containing a `ProgressSummaryCard` for each child, passing `child`, `progressByChild[child.id] ?? {}`, and `locale`
- Add a link to `/${locale}/vakanties` labeled "Vakantierooster" in the header action buttons (between Abonnement and logout). Style: `bg-sky-500 text-white font-bold py-3 px-6 rounded-xl hover:bg-sky-600`
- Import ProgressSummaryCard from `@/components/progress/ProgressSummaryCard`
- Import ChildSubjectProgress type from `@/types/progress`

**Do NOT change:**
- The authentication logic
- The subscription check logic
- The no-subscription fallback UI
- The AddChildForm or ChildList components (OUDER-02 already works)
- The existing Abonnement link (OUDER-03 already works)
  </action>
  <verify>
Run `npx tsc --noEmit` from aruba-leren/ — no type errors. Run `npm run build` from aruba-leren/ — build succeeds. Verify dashboard page still renders (check build output for /dashboard route).
  </verify>
  <done>
Parent dashboard shows progress summary cards for each child with per-subject level display. Links exist to per-child detail page, subscription status (OUDER-03), and vakantierooster. Children can still be added/edited (OUDER-02). Build passes.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with no errors
- Dashboard page route exists in build output
- Per-child detail page route `/dashboard/kind/[childId]` exists in build output
- ProgressSummaryCard component imports and uses existing SubjectProgress
- Dashboard uses batch `.in('child_id', childIds)` query (no N+1)
- Per-child page uses `profile.id` for parent_id ownership check (not user.id)
</verification>

<success_criteria>
- Parent dashboard displays progress cards per child showing subject levels and stuck indicators
- Per-child detail page shows full subject breakdown with session counts and recent activity
- Existing functionality preserved: add/edit children, subscription link, Koko CTA
- Mobile-first responsive layout (1 col mobile, 2 col desktop)
- No N+1 queries — progress fetched in single batch
</success_criteria>

<output>
After completion, create `.planning/phases/07-parent-portal-admin-monitoring/07-01-SUMMARY.md`
</output>
