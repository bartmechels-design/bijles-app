---
phase: 07-parent-portal-admin-monitoring
plan: 03
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - aruba-leren/src/app/[locale]/admin/gebruikers/page.tsx
  - aruba-leren/src/app/[locale]/admin/page.tsx
autonomous: true

must_haves:
  truths:
    - "Admin sees an overview of all families with their children and subscription status"
    - "Admin sees a prominent alert section for stuck children (is_stuck = true) with child name, family, and subject"
    - "Admin landing page has active links to Gebruikers and Vakanties pages (not placeholder cards)"
  artifacts:
    - path: "aruba-leren/src/app/[locale]/admin/gebruikers/page.tsx"
      provides: "Admin families overview with stuck alerts"
      min_lines: 50
    - path: "aruba-leren/src/app/[locale]/admin/page.tsx"
      provides: "Updated admin landing with active links"
      contains: "admin/gebruikers"
  key_links:
    - from: "aruba-leren/src/app/[locale]/admin/gebruikers/page.tsx"
      to: "profiles + children + child_subject_progress + subscriptions"
      via: "createAdminClient() with nested select"
      pattern: "createAdminClient"
    - from: "aruba-leren/src/app/[locale]/admin/page.tsx"
      to: "aruba-leren/src/app/[locale]/admin/gebruikers/page.tsx"
      via: "Link href"
      pattern: "admin/gebruikers"
---

<objective>
Build the admin families overview page with stuck-child alerts and update the admin landing page links.

Purpose: Admin needs to monitor all families and children (ADMIN-03) and receive alerts when children are stuck on concepts 3x (ADMIN-04). The admin landing page currently has placeholder cards for "Gebruikers" and "Statistieken" — these become active links.

Output: Admin gebruikers page with family overview + stuck alerts, updated admin landing page with active links to all admin sections.
</objective>

<execution_context>
@C:/Users/Familie Mechels/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Familie Mechels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@aruba-leren/src/app/[locale]/admin/page.tsx
@aruba-leren/src/app/[locale]/admin/layout.tsx
@aruba-leren/src/lib/auth/admin.ts
@aruba-leren/src/types/progress.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin gebruikers page with families overview and stuck alerts</name>
  <files>
    aruba-leren/src/app/[locale]/admin/gebruikers/page.tsx
  </files>
  <action>
Create a Server Component page at `/admin/gebruikers/page.tsx`:

**Authentication:**
- Import `isAdmin`, `createAdminClient` from `@/lib/auth/admin`
- Check `isAdmin()` — redirect to `/${locale}` if not admin

**Data fetching with nested joins:**
```typescript
const adminClient = createAdminClient()
const { data: families } = await adminClient
  .from('profiles')
  .select(`
    id,
    display_name,
    user_id,
    created_at,
    children ( id, first_name, grade, child_subject_progress ( subject, current_level, is_stuck, stuck_since, last_session_at, total_sessions ) ),
    subscriptions ( status, expires_at )
  `)
  .order('created_at', { ascending: false })
```

**Stuck alerts extraction (ADMIN-04):**
- Flatten families data to find all children where any `child_subject_progress` row has `is_stuck = true`
- Build array of alert objects: `{ familyName, childName, childGrade, subject, stuckSince }`
- Display at TOP of page in a red alert box (`bg-red-50 border-l-4 border-red-500 rounded-xl p-4 mb-8`)
- Header: "Kinderen die vastgelopen zijn ({count})" in bold red
- Each alert as list item: "{childName} ({familyName}) - {subject} (sinds {formatted stuckSince date})"
- If no stuck children, show green success box: "Geen kinderen vastgelopen" with checkmark

**Families overview (ADMIN-03):**
- Page title: "Families & Kinderen" (text-3xl font-bold)
- Summary stats row: total families count, total children count, active subscriptions count
- For each family, render a card (`bg-white rounded-xl shadow-md p-6 border-l-4 border-sky-500`):
  - Family display_name (text-lg font-bold)
  - Subscription status badge: "Actief" (green), "Verlopen" (red), or "Geen" (gray) — check status + expires_at
  - Member since date (created_at formatted)
  - Children as rounded pill badges: "{first_name} (klas {grade})" in sky-100/sky-800
  - For each child, show subject count with assessments completed and any stuck indicators as small inline badges
- Mobile-first: single column cards, stack vertically
- If no families exist, show "Geen families gevonden" empty state
  </action>
  <verify>
Run `npx tsc --noEmit` from aruba-leren/ — no type errors. Verify the nested Supabase select syntax is correct (children → child_subject_progress FK chain).
  </verify>
  <done>
Admin sees all families with children, subscription status, and progress indicators. Stuck children (is_stuck=true) are prominently displayed at the top in a red alert box with child name, family, subject, and stuck-since date.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update admin landing page with active links</name>
  <files>
    aruba-leren/src/app/[locale]/admin/page.tsx
  </files>
  <action>
Update the existing admin landing page to replace placeholder cards with active links:

**Replace the "Gebruikers" placeholder card** (currently `bg-gray-100 opacity-50 border-l-4 border-gray-300`) with an active Link card:
- Link to `/${locale}/admin/gebruikers`
- Style matching the existing Betalingen card pattern: `bg-white rounded-xl shadow-md hover:shadow-xl transition-shadow p-6 border-l-4 border-sky-500`
- Icon: people/users SVG (reuse the existing SVG that's already in the placeholder)
- Title: "Gebruikers" (text-xl font-bold text-gray-900)
- Subtitle: "Families en kinderen beheren"
- Remove `opacity-50` and `bg-gray-100`

**Replace the "Statistieken" placeholder card** with a "Vakanties" active Link card:
- Link to `/${locale}/admin/vakanties`
- Style: `bg-white rounded-xl shadow-md hover:shadow-xl transition-shadow p-6 border-l-4 border-purple-500`
- Icon: calendar SVG (path: M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z)
- Title: "Vakanties" (text-xl font-bold text-gray-900)
- Subtitle: "Schoolvakanties beheren"

Keep the existing Betalingen and Leerstof cards unchanged. The grid remains `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`.
  </action>
  <verify>
Run `npm run build` from aruba-leren/ — build succeeds. Verify `/admin` route in build output. Verify `/admin/gebruikers` route in build output.
  </verify>
  <done>
Admin landing page shows four active cards: Betalingen, Leerstof, Gebruikers, and Vakanties. No more placeholder cards with "Binnenkort beschikbaar". All cards link to their respective admin pages.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `/admin/gebruikers` route exists in build output
- Admin landing has 4 active Link cards (no more gray placeholders)
- Gebruikers page uses `createAdminClient()` (bypasses RLS for cross-family data)
- Stuck alerts derived from `is_stuck = true` in child_subject_progress
- All admin pages check `isAdmin()` before rendering
</verification>

<success_criteria>
- Admin families overview shows all families with children, subscriptions, and progress
- Stuck children appear in prominent red alert section at top of gebruikers page
- Admin landing page has active links to all 4 sections: Betalingen, Leerstof, Gebruikers, Vakanties
- No placeholder/disabled cards remain on admin landing
- Mobile-first responsive layout
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/07-parent-portal-admin-monitoring/07-03-SUMMARY.md`
</output>
