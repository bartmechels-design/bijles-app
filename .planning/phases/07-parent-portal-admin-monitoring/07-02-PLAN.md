---
phase: 07-parent-portal-admin-monitoring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - aruba-leren/supabase/migrations/009_vacation_tables.sql
  - aruba-leren/src/lib/vacations/queries.ts
  - aruba-leren/src/lib/vacations/actions.ts
  - aruba-leren/src/app/[locale]/vakanties/page.tsx
  - aruba-leren/src/app/[locale]/admin/vakanties/page.tsx
  - aruba-leren/src/components/admin/VacationForm.tsx
autonomous: true

must_haves:
  truths:
    - "Parent can view the current school year vacation calendar at /vakanties"
    - "Admin can add, edit, and delete vacation entries at /admin/vakanties"
    - "Vacation dates are scoped by school year (only current year shown to parent)"
    - "Vacation data persists in database with RLS (authenticated read, admin write via service role)"
  artifacts:
    - path: "aruba-leren/supabase/migrations/009_vacation_tables.sql"
      provides: "school_vacations table with RLS"
      contains: "CREATE TABLE"
    - path: "aruba-leren/src/lib/vacations/queries.ts"
      provides: "getVacations query function"
      exports: ["getVacations"]
    - path: "aruba-leren/src/lib/vacations/actions.ts"
      provides: "Server Actions for vacation CRUD"
      exports: ["upsertVacation", "deleteVacation"]
    - path: "aruba-leren/src/app/[locale]/vakanties/page.tsx"
      provides: "Parent-facing vacation calendar page"
      min_lines: 30
    - path: "aruba-leren/src/app/[locale]/admin/vakanties/page.tsx"
      provides: "Admin vacation management page"
      min_lines: 40
    - path: "aruba-leren/src/components/admin/VacationForm.tsx"
      provides: "Add/edit vacation form component"
      min_lines: 30
  key_links:
    - from: "aruba-leren/src/app/[locale]/vakanties/page.tsx"
      to: "school_vacations"
      via: "Supabase query filtered by current school year"
      pattern: "\\.eq\\('school_year'"
    - from: "aruba-leren/src/lib/vacations/actions.ts"
      to: "school_vacations"
      via: "createAdminClient() for insert/update/delete"
      pattern: "createAdminClient"
---

<objective>
Build the complete vacation calendar system: database table, parent view, and admin CRUD.

Purpose: Parents need to see Arubaanse schoolvakanties (OUDER-05) and admin needs to manage the vacation schedule (ADMIN-05). This is a full vertical slice: migration + data layer + parent read-only view + admin management UI.

Output: Migration 009 with school_vacations table, parent-facing calendar at `/vakanties`, admin CRUD at `/admin/vakanties`, and Server Actions for vacation management.
</objective>

<execution_context>
@C:/Users/Familie Mechels/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Familie Mechels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@aruba-leren/src/lib/auth/admin.ts
@aruba-leren/src/app/[locale]/admin/layout.tsx
@aruba-leren/src/app/[locale]/admin/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create vacation migration, queries, and Server Actions</name>
  <files>
    aruba-leren/supabase/migrations/009_vacation_tables.sql
    aruba-leren/src/lib/vacations/queries.ts
    aruba-leren/src/lib/vacations/actions.ts
  </files>
  <action>
**Migration 009** — Create `school_vacations` table:
```sql
CREATE TABLE IF NOT EXISTS school_vacations (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL,
  start_date DATE NOT NULL,
  end_date DATE NOT NULL,
  school_year TEXT NOT NULL,
  is_public_holiday BOOLEAN NOT NULL DEFAULT false,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  CONSTRAINT valid_date_range CHECK (end_date >= start_date)
);
CREATE INDEX IF NOT EXISTS idx_school_vacations_year ON school_vacations(school_year, start_date ASC);
ALTER TABLE school_vacations ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Authenticated users can view school vacations" ON school_vacations FOR SELECT USING (auth.role() = 'authenticated');
CREATE TRIGGER update_school_vacations_updated_at BEFORE UPDATE ON school_vacations FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
```

**queries.ts** — Export `getVacations(schoolYear: string)` function:
- Uses regular Supabase server client (RLS allows authenticated read)
- Queries `school_vacations` filtered by `school_year`, ordered by `start_date` ascending
- Export a helper `getCurrentSchoolYear()`: Aruba school year starts August. If month >= 7 (August = index 7), year is `${currentYear}-${currentYear+1}`, else `${currentYear-1}-${currentYear}`
- Return typed array

**actions.ts** — Server Actions with `'use server'` directive:
- `upsertVacation(formData: FormData)`: Extract id (nullable), name, start_date, end_date, school_year, is_public_holiday from FormData. Check `isAdmin()` — return `{ error: 'Geen toegang' }` if not admin. Use `createAdminClient()` for insert (no id) or update (has id). Call `revalidatePath` for both `/nl/admin/vakanties` and `/nl/vakanties`. Return `{ success: true }`.
- `deleteVacation(id: string)`: Check `isAdmin()`. Use `createAdminClient()` to delete by id. Revalidate same paths. Return `{ success: true }`.
- Validate: name non-empty, start_date and end_date valid dates, end_date >= start_date. Return `{ error: string }` on validation failure.
  </action>
  <verify>
Run `npx tsc --noEmit` from aruba-leren/ — no type errors. Verify migration SQL is syntactically valid (no runtime test needed — user runs manually in Supabase SQL Editor).
  </verify>
  <done>
Migration 009 creates school_vacations with RLS, index, and updated_at trigger. Query function fetches vacations by school year. Server Actions handle upsert/delete with admin check.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create parent vacation page and admin vacation management page</name>
  <files>
    aruba-leren/src/app/[locale]/vakanties/page.tsx
    aruba-leren/src/app/[locale]/admin/vakanties/page.tsx
    aruba-leren/src/components/admin/VacationForm.tsx
  </files>
  <action>
**Parent vacation page** (`/vakanties/page.tsx`) — Server Component:
- Import `createClient` from `@/lib/supabase/server`, `getVacations` and `getCurrentSchoolYear` from `@/lib/vacations/queries`
- Authenticate user (redirect to login if not authenticated)
- Get current school year via `getCurrentSchoolYear()`
- Fetch vacations via `getVacations(currentSchoolYear)`
- Render: page title "Vakantierooster {schoolYear}", list of vacation cards
- Each card: `bg-white rounded-xl p-4 shadow-sm border-l-4 border-sky-400` with vacation name (font-semibold), formatted date range "DD maand YYYY t/m DD maand YYYY" using `toLocaleDateString('nl-NL', { day: 'numeric', month: 'long', year: 'numeric' })`, and optional "Feestdag" amber badge if is_public_holiday
- Empty state: "Geen vakanties gevonden voor dit schooljaar" in gray italic
- Mobile-first: max-w-2xl mx-auto px-4 py-8
- Back link to `/${locale}/dashboard`

**Admin vacation management page** (`/admin/vakanties/page.tsx`) — Server Component:
- Check `isAdmin()` — redirect if not admin
- Use `createAdminClient()` to fetch ALL vacations (not just current year), ordered by school_year DESC then start_date ASC
- Render: page title "Vakantierooster Beheer"
- Add new vacation button that shows VacationForm (use client component with useState toggle)
- List existing vacations grouped by school_year, each with edit and delete buttons
- Edit uses VacationForm pre-filled with existing data
- Delete calls `deleteVacation` Server Action with confirmation

**VacationForm.tsx** — `'use client'` component:
- Props: `{ vacation?: { id, name, start_date, end_date, school_year, is_public_holiday } | null, onClose: () => void }`
- Form fields: name (text input), start_date (date input), end_date (date input), school_year (text input, default to current school year), is_public_holiday (checkbox)
- Submit calls `upsertVacation` Server Action via FormData
- Show loading state during submission
- Call `onClose()` after successful save
- Style consistent with existing admin UI (white card, rounded, shadow)
  </action>
  <verify>
Run `npx tsc --noEmit` from aruba-leren/ — no type errors. Run `npm run build` — build succeeds. Verify `/vakanties` and `/admin/vakanties` routes appear in build output.
  </verify>
  <done>
Parent can view vacation calendar filtered by current school year at /vakanties. Admin can add, edit, and delete vacation entries at /admin/vakanties. VacationForm handles both create and edit modes.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `/vakanties` route exists in build output
- `/admin/vakanties` route exists in build output
- Migration 009 SQL file exists and has valid syntax
- Parent page filters by school year
- Admin operations use `createAdminClient()` (service role, bypasses RLS)
- Admin check via `isAdmin()` on all write operations
</verification>

<success_criteria>
- school_vacations table migration ready to run (user runs manually in Supabase SQL Editor)
- Parent vacation page shows current school year vacations with date range and holiday badges
- Admin can add new vacation entries with name, dates, school year, and holiday flag
- Admin can edit existing entries and delete with confirmation
- All admin writes use service role client; all admin routes check isAdmin()
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-parent-portal-admin-monitoring/07-02-SUMMARY.md`
</output>
