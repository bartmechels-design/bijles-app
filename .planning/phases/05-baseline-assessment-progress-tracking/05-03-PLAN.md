---
phase: 05-baseline-assessment-progress-tracking
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - aruba-leren/src/components/progress/ProgressBar.tsx
  - aruba-leren/src/components/progress/LevelBadge.tsx
  - aruba-leren/src/components/progress/SubjectProgress.tsx
  - aruba-leren/src/components/tutor/SubjectSelector.tsx
  - aruba-leren/src/app/[locale]/tutor/[childId]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Child sees progress bar showing current level (1-5) per subject"
    - "Child sees level badge with kid-friendly name (Leerling-Aap through Super-Aap)"
    - "Subject cards show 'Toets nodig' indicator when assessment not yet completed"
    - "Subject cards link to assessment page when assessment not completed, to tutoring page when completed"
    - "Stuck indicator visible on subject card when is_stuck=true"
  artifacts:
    - path: "aruba-leren/src/components/progress/ProgressBar.tsx"
      provides: "Tailwind progress bar component (1-5 level)"
      min_lines: 20
    - path: "aruba-leren/src/components/progress/LevelBadge.tsx"
      provides: "Kid-friendly level badge with locale support"
      min_lines: 25
    - path: "aruba-leren/src/components/progress/SubjectProgress.tsx"
      provides: "Combined progress card per subject (bar + badge + stuck indicator)"
      min_lines: 30
    - path: "aruba-leren/src/components/tutor/SubjectSelector.tsx"
      provides: "Subject cards with progress data and assessment gate"
      contains: "assessment_completed"
  key_links:
    - from: "aruba-leren/src/app/[locale]/tutor/[childId]/page.tsx"
      to: "aruba-leren/src/lib/tutoring/assessment-manager.ts"
      via: "getAllSubjectProgress() server fetch"
      pattern: "getAllSubjectProgress"
    - from: "aruba-leren/src/components/tutor/SubjectSelector.tsx"
      to: "aruba-leren/src/components/progress/SubjectProgress.tsx"
      via: "renders SubjectProgress inside each subject card"
      pattern: "SubjectProgress"
---

<objective>
Create kid-friendly progress visualization components and integrate assessment gate into the subject selection flow.

Purpose: Children see their level and progress per subject. Subject selection routes to assessment when needed.
Output: ProgressBar, LevelBadge, SubjectProgress components. Updated SubjectSelector with progress data and assessment gate.
</objective>

<execution_context>
@C:/Users/Familie Mechels/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Familie Mechels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-baseline-assessment-progress-tracking/05-RESEARCH.md
@.planning/phases/05-baseline-assessment-progress-tracking/05-01-SUMMARY.md
@aruba-leren/src/components/tutor/SubjectSelector.tsx
@aruba-leren/src/app/[locale]/tutor/[childId]/page.tsx
@aruba-leren/src/types/progress.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Progress visualization components</name>
  <files>
    aruba-leren/src/components/progress/ProgressBar.tsx
    aruba-leren/src/components/progress/LevelBadge.tsx
    aruba-leren/src/components/progress/SubjectProgress.tsx
  </files>
  <action>
    Create three components in `src/components/progress/`. All are client components ('use client') since they use locale-dependent rendering.

    1. `ProgressBar.tsx`:
       - Props: `{ level: number; maxLevel?: number; label?: string; showLevel?: boolean }`
       - Pure Tailwind CSS progress bar. Level 1-5 maps to 0%-100% width.
       - Use gradient: `from-sky-400 to-sky-600` (matching app theme).
       - Include `role="progressbar"` with `aria-valuenow`, `aria-valuemin`, `aria-valuemax` for accessibility.
       - Smooth transition: `transition-all duration-500`.
       - Height: `h-4` with rounded-full. Background: `bg-gray-200`.
       - Optional label above bar with level indicator (e.g., "3/5").

    2. `LevelBadge.tsx`:
       - Props: `{ level: number; locale: string }`
       - Import LEVEL_NAMES from `@/types/progress`
       - Display level name in correct locale (nl/pap/es/en) with color-coded badge.
       - Colors per level: 1=green, 2=blue, 3=yellow, 4=orange, 5=purple (each with bg-X-100, text-X-800, border-X-300 pattern).
       - Include emoji per level: 1=seedling, 2=star, 3=glowing star, 4=trophy, 5=rocket.
       - Use `inline-flex items-center gap-1 px-3 py-1 rounded-full border-2 font-bold text-sm`.

    3. `SubjectProgress.tsx`:
       - Props: `{ progress: ChildSubjectProgress | null; locale: string }`
       - Import ProgressBar and LevelBadge.
       - When progress is null: show "Toets nodig" badge (amber background, with clipboard emoji).
       - When progress exists and assessment_completed:
         - Show LevelBadge with current_level
         - Show ProgressBar with current_level
         - If is_stuck: show a small red "Vastgelopen" indicator (red-100 background with warning emoji)
       - When progress exists but assessment_completed=false: show "Toets nodig" badge.
       - Keep it compact — this sits inside a subject card.
  </action>
  <verify>
    Run `npx tsc --noEmit` from aruba-leren/. All three components compile. Verify they import from correct paths.
  </verify>
  <done>
    Three progress components exist: ProgressBar (accessible Tailwind bar), LevelBadge (locale-aware kid-friendly badge), SubjectProgress (combined card with stuck indicator and assessment-needed state).
  </done>
</task>

<task type="auto">
  <name>Task 2: SubjectSelector assessment gate + progress display</name>
  <files>
    aruba-leren/src/components/tutor/SubjectSelector.tsx
    aruba-leren/src/app/[locale]/tutor/[childId]/page.tsx
  </files>
  <action>
    1. Update `src/app/[locale]/tutor/[childId]/page.tsx` (Server Component):
       - Import `getAllSubjectProgress` from `@/lib/tutoring/assessment-manager`
       - After verifying child ownership, fetch progress: `const progressRows = await getAllSubjectProgress(childId)`
       - Build a progressMap: `Record<string, ChildSubjectProgress>` keyed by subject
       - Pass `progressMap` as prop to SubjectSelector

    2. Update `src/components/tutor/SubjectSelector.tsx`:
       - Add `progressMap: Record<string, ChildSubjectProgress>` to props (import ChildSubjectProgress from @/types/progress)
       - For each subject card:
         a. Look up progress: `const progress = progressMap[subject.id] || null`
         b. Determine link destination:
            - If progress?.assessment_completed === true: link to `/${locale}/tutor/${childId}/${subject.id}` (tutoring)
            - Otherwise: link to `/${locale}/assessment/${childId}/${subject.id}` (assessment)
         c. Render SubjectProgress component inside the card, below the subject label and category badge
         d. Update the button text: if assessment not completed, show "Start Toets" instead of "Start Sessie" (use i18n key `tutor.startAssessment`, fall back to "Start Toets")
       - Import SubjectProgress from `@/components/progress/SubjectProgress`
       - Keep the existing grid layout and card styling. The SubjectProgress component fits naturally below the category badge.
  </action>
  <verify>
    Run `npx tsc --noEmit` from aruba-leren/. SubjectSelector compiles with new props. Subject page passes progressMap. Verify that assessment_completed check determines link destination.
  </verify>
  <done>
    Subject cards show progress visualization (level badge + progress bar) when assessment is completed. Cards show "Toets nodig" when assessment not done. Cards link to assessment page when assessment needed, tutoring page when completed. Stuck indicator visible when child is stuck.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- ProgressBar renders with aria attributes
- LevelBadge shows correct locale name for each level 1-5
- SubjectProgress shows three states: null (toets nodig), completed (badge+bar), stuck (red indicator)
- SubjectSelector links to /assessment/ when assessment_completed is false
- SubjectSelector links to /tutor/ when assessment_completed is true
- Subject selection page fetches and passes progressMap
</verification>

<success_criteria>
- Child sees level badge and progress bar on each subject card after completing assessment
- Subject cards clearly indicate when assessment is needed vs ready for tutoring
- Stuck subjects show visual warning
- All progress components use kid-friendly language matching locale
</success_criteria>

<output>
After completion, create `.planning/phases/05-baseline-assessment-progress-tracking/05-03-SUMMARY.md`
</output>
