---
phase: 05-baseline-assessment-progress-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - aruba-leren/supabase/migrations/007_assessment_progress_tables.sql
  - aruba-leren/src/types/tutoring.ts
  - aruba-leren/src/types/progress.ts
  - aruba-leren/src/lib/tutoring/assessment-manager.ts
  - aruba-leren/src/lib/tutoring/progress-tracker.ts
autonomous: true
user_setup:
  - service: supabase
    why: "New tables and column for assessment and progress tracking"
    dashboard_config:
      - task: "Run migration 007_assessment_progress_tables.sql in SQL Editor"
        location: "Supabase Dashboard > SQL Editor"

must_haves:
  truths:
    - "SQL migration file exists with child_subject_progress and progress_events tables plus session_type column on tutoring_sessions"
    - "Assessment manager can check progress, create assessment sessions, and finalize assessments"
    - "Progress tracker can update stuck flags and record level change events"
    - "TypeScript types cover all new database entities"
  artifacts:
    - path: "aruba-leren/supabase/migrations/007_assessment_progress_tables.sql"
      provides: "Database schema for assessment and progress tracking"
      contains: "child_subject_progress"
    - path: "aruba-leren/src/types/progress.ts"
      provides: "TypeScript types for progress entities"
      exports: ["ChildSubjectProgress", "ProgressEvent", "ProgressEventType"]
    - path: "aruba-leren/src/lib/tutoring/assessment-manager.ts"
      provides: "Assessment session lifecycle management"
      exports: ["getChildSubjectProgress", "createAssessmentSession", "finishAssessment", "getAllSubjectProgress"]
    - path: "aruba-leren/src/lib/tutoring/progress-tracker.ts"
      provides: "Progress tracking and stuck detection"
      exports: ["updateStuckFlag", "recordProgressEvent"]
  key_links:
    - from: "aruba-leren/src/lib/tutoring/assessment-manager.ts"
      to: "child_subject_progress table"
      via: "supabase upsert on child_id+subject unique constraint"
      pattern: "upsert.*child_subject_progress"
    - from: "aruba-leren/src/lib/tutoring/progress-tracker.ts"
      to: "progress_events table"
      via: "supabase insert"
      pattern: "from\\('progress_events'\\)\\.insert"
---

<objective>
Create the database migration, TypeScript types, and server-side utility modules for assessment sessions and progress tracking.

Purpose: All other Phase 5 plans depend on these tables and functions. This is the data foundation.
Output: SQL migration file (user runs manually), types, assessment-manager.ts, progress-tracker.ts
</objective>

<execution_context>
@C:/Users/Familie Mechels/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Familie Mechels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-baseline-assessment-progress-tracking/05-RESEARCH.md
@aruba-leren/src/types/tutoring.ts
@aruba-leren/src/lib/tutoring/session-manager.ts
@aruba-leren/supabase/migrations/006_tutoring_tables.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: SQL migration + TypeScript types</name>
  <files>
    aruba-leren/supabase/migrations/007_assessment_progress_tables.sql
    aruba-leren/src/types/tutoring.ts
    aruba-leren/src/types/progress.ts
  </files>
  <action>
    1. Create `007_assessment_progress_tables.sql` with the EXACT SQL from 05-RESEARCH.md "Supabase SQL" section:
       - ALTER tutoring_sessions: add `session_type TEXT NOT NULL DEFAULT 'tutoring' CHECK (session_type IN ('assessment', 'tutoring'))`
       - CREATE TABLE child_subject_progress (id, child_id, subject, current_level, assessment_completed, assessment_session_id, total_sessions, total_correct, total_incorrect, total_hints_received, is_stuck, stuck_since, stuck_concept_count, last_session_at, created_at, updated_at, UNIQUE(child_id, subject))
       - CREATE TABLE progress_events (id, child_id, subject, session_id, event_type, level_at_event, metadata JSONB, created_at)
       - All indexes from research
       - RLS enabled on both new tables with parent-owns-child policy pattern (same as tutoring_sessions)
       - Add a comment header: "Run in: Supabase Dashboard > SQL Editor"

    2. Extend `src/types/tutoring.ts`:
       - Add `session_type?: 'assessment' | 'tutoring'` to TutoringSession interface
       - Add `assessment_questions_asked?: number` to SessionMetadata interface

    3. Create `src/types/progress.ts`:
       - Export `ChildSubjectProgress` interface matching the child_subject_progress table columns
       - Export `ProgressEvent` interface matching the progress_events table columns
       - Export `ProgressEventType` as a union type: 'assessment_completed' | 'level_up' | 'level_down' | 'stuck_flagged' | 'stuck_cleared' | 'session_started' | 'session_ended'
       - Export `LEVEL_NAMES` constant: Record<number, { nl: string; pap: string; es: string; en: string }> mapping levels 1-5 to kid-friendly names (Leerling-Aap, Junior-Aap, Gewone-Aap, Senior-Aap, Super-Aap with translations)
  </action>
  <verify>
    Run `npx tsc --noEmit` from aruba-leren/ — no type errors. Confirm migration file exists with correct SQL. Confirm progress.ts exports are importable.
  </verify>
  <done>
    Migration file ready for user to run. TutoringSession type has session_type. ChildSubjectProgress and ProgressEvent types defined. LEVEL_NAMES constant available for UI components.
  </done>
</task>

<task type="auto">
  <name>Task 2: Assessment manager and progress tracker modules</name>
  <files>
    aruba-leren/src/lib/tutoring/assessment-manager.ts
    aruba-leren/src/lib/tutoring/progress-tracker.ts
  </files>
  <action>
    1. Create `assessment-manager.ts` with these functions (use patterns from 05-RESEARCH.md code examples):
       - `getChildSubjectProgress(childId, subject)` — query child_subject_progress, return row or null. Use `.maybeSingle()`.
       - `getAllSubjectProgress(childId)` — query all rows for a child. Returns array of ChildSubjectProgress. Used by SubjectSelector to show progress on all subjects at once.
       - `createAssessmentSession(childId, subject)` — insert into tutoring_sessions with `session_type: 'assessment'`, `difficulty_level: 3` (CAT start), metadata with `assessment_questions_asked: 0`, `igdi_phase: 'diagnostische_toets'`. Return the session.
       - `finishAssessment(childId, subject, determinedLevel, sessionId)` — UPSERT child_subject_progress (onConflict: 'child_id,subject') with current_level, assessment_completed=true, assessment_session_id. Insert progress_event with event_type='assessment_completed'. End the session (set ended_at). Make idempotent: if session already has ended_at, skip.
       - All functions use `createClient` from `@/lib/supabase/server`.
       - Import types from `@/types/tutoring` and `@/types/progress`.

    2. Create `progress-tracker.ts` with these functions:
       - `updateStuckFlag(childId, subject, isStuck)` — if isStuck=true: upsert child_subject_progress with is_stuck=true, stuck_since=now, increment stuck_concept_count via a raw SQL increment or read-modify-write. If isStuck=false: update is_stuck=false, stuck_since=null. Only update rows where child_id+subject match.
       - `recordProgressEvent(childId, subject, sessionId, eventType, newLevel, reason)` — upsert child_subject_progress.current_level to newLevel. Insert progress_event with event_type and metadata={reason}.
       - Both use `createClient` from `@/lib/supabase/server`.

    IMPORTANT: For stuck_concept_count increment, do a read-then-write approach (read current value, write +1) rather than an RPC call. Simpler and avoids needing a Supabase function. The race condition window is negligible for this use case.
  </action>
  <verify>
    Run `npx tsc --noEmit` from aruba-leren/ — no type errors. Check that assessment-manager.ts and progress-tracker.ts compile cleanly and import their dependencies correctly.
  </verify>
  <done>
    assessment-manager.ts exports getChildSubjectProgress, getAllSubjectProgress, createAssessmentSession, finishAssessment. progress-tracker.ts exports updateStuckFlag, recordProgressEvent. All functions use correct Supabase patterns matching existing session-manager.ts conventions.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- Migration file contains: ALTER TABLE tutoring_sessions, CREATE TABLE child_subject_progress, CREATE TABLE progress_events, RLS policies, indexes
- Types in progress.ts align with SQL column names
- assessment-manager.ts and progress-tracker.ts use same Supabase client pattern as session-manager.ts
</verification>

<success_criteria>
- SQL migration 007 ready for manual execution
- TutoringSession type extended with session_type
- ChildSubjectProgress and ProgressEvent types defined
- assessment-manager.ts and progress-tracker.ts compile and export all required functions
</success_criteria>

<output>
After completion, create `.planning/phases/05-baseline-assessment-progress-tracking/05-01-SUMMARY.md`
</output>
