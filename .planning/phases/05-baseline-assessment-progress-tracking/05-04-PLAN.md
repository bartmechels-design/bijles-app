---
phase: 05-baseline-assessment-progress-tracking
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Assessment flow works end-to-end: subject card -> assessment page -> Koko asks questions -> level determined -> redirect to tutoring"
    - "Progress visualization shows correct level after assessment"
    - "Stuck detection visible after 3 consecutive incorrect answers"
    - "Existing tutoring flow not broken by Phase 5 changes"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification of the assessment and progress tracking flow.

Purpose: Confirm all Phase 5 features work together before marking phase complete.
Output: Verification results and any fixes needed.
</objective>

<execution_context>
@C:/Users/Familie Mechels/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Familie Mechels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-baseline-assessment-progress-tracking/05-RESEARCH.md
@.planning/phases/05-baseline-assessment-progress-tracking/05-02-SUMMARY.md
@.planning/phases/05-baseline-assessment-progress-tracking/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and smoke test</name>
  <files></files>
  <action>
    1. Run `npm run build` from aruba-leren/ to verify the entire app builds without errors.
    2. Check for TypeScript errors: `npx tsc --noEmit`
    3. Verify all new files exist:
       - supabase/migrations/007_assessment_progress_tables.sql
       - src/types/progress.ts
       - src/lib/tutoring/assessment-manager.ts
       - src/lib/tutoring/progress-tracker.ts
       - src/lib/ai/prompts/system-prompts.ts (updated with buildAssessmentPrompt)
       - src/app/[locale]/assessment/[childId]/[subject]/page.tsx
       - src/components/progress/ProgressBar.tsx
       - src/components/progress/LevelBadge.tsx
       - src/components/progress/SubjectProgress.tsx
    4. Verify route.ts contains: finishAssessment import, updateStuckFlag import, recordProgressEvent import, ASSESSMENT_DONE regex
    5. Verify ChatInterface accepts sessionType prop
    6. Verify SubjectSelector accepts progressMap prop and has assessment gate logic

    If any issues found, fix them immediately (minor fixes only — routing, imports, typos). If major issues found, document them for the checkpoint.
  </action>
  <verify>
    `npm run build` succeeds. `npx tsc --noEmit` passes. All files listed above exist.
  </verify>
  <done>
    App builds successfully with all Phase 5 code. No TypeScript errors. All required files present and correctly wired.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete assessment and progress flow</name>
  <action>
    Present the verification steps to the user. The user needs to:
    1. Run SQL migration 007 in Supabase Dashboard SQL Editor
    2. Start dev server and test the full assessment flow
    3. Verify progress visualization after assessment
    4. Verify existing tutoring is not broken

    Complete Phase 5: Baseline Assessment and Progress Tracking system including:
    - SQL migration 007 (user needs to run this first!)
    - Assessment flow: /assessment/[childId]/[subject] page with Koko assessment chat
    - Assessment completion detection via [ASSESSMENT_DONE:level=X]
    - Progress tracking: child_subject_progress table with level, stuck flag
    - Progress UI: level badges, progress bars on subject selection cards
    - Assessment gate: subjects link to assessment when not yet completed
    - Stuck detection: 3x consecutive incorrect sets is_stuck flag
  </action>
  <verify>
    User follows these steps:

    STAP 1: Run SQL migration
    - Open Supabase Dashboard > SQL Editor
    - Copy and paste contents of aruba-leren/supabase/migrations/007_assessment_progress_tables.sql
    - Click "Run" — should complete without errors
    - Verify in Table Editor: child_subject_progress and progress_events tables exist
    - Verify tutoring_sessions now has session_type column

    STAP 2: Start dev server
    - Run `npm run dev` in aruba-leren/
    - Open http://localhost:3000

    STAP 3: Test assessment flow
    - Log in as parent, navigate to tutor page, select a child
    - Subject cards should show "Toets nodig" indicator on all subjects (no assessments done yet)
    - Click on a subject — should redirect to /assessment/[childId]/[subject] (NOT /tutor/)
    - Koko should start asking assessment questions (different from normal tutoring)
    - Answer 5-7 questions — Koko should adapt difficulty
    - After final question, Koko should announce the level and show completion UI
    - Click "Ga naar de les" to navigate to tutoring page

    STAP 4: Verify progress display
    - Go back to subject selection page
    - The subject you just assessed should now show a level badge (e.g., "Junior-Aap") and progress bar
    - Other subjects should still show "Toets nodig"
    - Clicking the assessed subject should go to /tutor/ (not /assessment/)

    STAP 5: Test existing tutoring (regression check)
    - Start a tutoring session on the assessed subject
    - Verify normal Koko behavior — Socratic method, hints, etc.
    - Verify the session starts at the assessed level

    STAP 6: (Optional) Test stuck detection
    - Give 3 wrong answers in a row
    - Go back to subject selection — subject card may show "Vastgelopen" indicator
  </verify>
  <done>
    User confirms: assessment flow works end-to-end, progress is visible, existing tutoring unaffected.
  </done>
</task>

</tasks>

<verification>
- App builds and runs without errors
- SQL migration executes successfully in Supabase
- Assessment flow: select subject -> assessment page -> Koko assessment -> level determined -> redirect to tutoring
- Progress display: level badge + progress bar visible after assessment
- Assessment gate: subjects without assessment link to /assessment/, subjects with assessment link to /tutor/
- Regression: existing tutoring chat unaffected
</verification>

<success_criteria>
- User confirms assessment flow works end-to-end
- User confirms progress visualization is visible and correct
- User confirms existing tutoring is not broken
- Phase 5 all 6 success criteria met
</success_criteria>

<output>
After completion, create `.planning/phases/05-baseline-assessment-progress-tracking/05-04-SUMMARY.md`
</output>
