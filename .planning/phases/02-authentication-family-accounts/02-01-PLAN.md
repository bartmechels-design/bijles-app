---
phase: 02-authentication-family-accounts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - aruba-leren/supabase/migrations/002_auth_consent.sql
  - aruba-leren/src/lib/auth/actions.ts
autonomous: true

must_haves:
  truths:
    - "Parent signup creates auth user AND profile record automatically"
    - "Parent consent is captured and stored with timestamp"
    - "Auth functions return proper error messages for invalid credentials"
  artifacts:
    - path: "aruba-leren/supabase/migrations/002_auth_consent.sql"
      provides: "Consent fields and auth trigger"
      contains: "consent_given boolean"
    - path: "aruba-leren/src/lib/auth/actions.ts"
      provides: "Auth server actions"
      exports: ["signUp", "signIn", "signOut"]
  key_links:
    - from: "auth.users table"
      to: "profiles table"
      via: "database trigger on INSERT"
      pattern: "CREATE TRIGGER.*after_auth_user_created"
    - from: "aruba-leren/src/lib/auth/actions.ts"
      to: "@supabase/ssr"
      via: "createClient() import"
      pattern: "import.*createClient.*from.*supabase"
---

<objective>
Set up database schema for consent tracking and implement core authentication server actions for email/password signup and login.

Purpose: This establishes the foundation for all authentication flows. The consent fields fulfill AUTH-06 (required consent checkbox), and the auth trigger ensures every signup automatically creates a profile record. Server actions provide reusable functions for all auth UI.

Output: SQL migration with consent tracking + auth trigger, and TypeScript server actions for signUp/signIn/signOut.
</objective>

<execution_context>
@C:/Users/Familie Mechels/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Familie Mechels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Familie Mechels/Bijles App/.planning/PROJECT.md
@C:/Users/Familie Mechels/Bijles App/.planning/ROADMAP.md
@C:/Users/Familie Mechels/Bijles App/.planning/STATE.md
@C:/Users/Familie Mechels/Bijles App/.planning/phases/01-foundation-infrastructure/01-01-SUMMARY.md
@C:/Users/Familie Mechels/Bijles App/aruba-leren/src/lib/supabase/client.ts
@C:/Users/Familie Mechels/Bijles App/aruba-leren/src/lib/supabase/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend profiles schema and create auth trigger</name>
  <files>aruba-leren/supabase/migrations/002_auth_consent.sql</files>
  <action>
Create SQL migration file at `aruba-leren/supabase/migrations/002_auth_consent.sql` that:

1. Adds consent fields to profiles table:
   - `consent_given` BOOLEAN NOT NULL DEFAULT false
   - `consent_date` TIMESTAMPTZ (nullable - set only when consent_given is true)

2. Creates database trigger function `handle_new_user()` that:
   - Triggers on INSERT to `auth.users` table
   - Automatically creates a profile record in `public.profiles` with:
     - `id` = NEW.id (from auth.users)
     - `role` = 'parent' (default for all signups)
     - `display_name` = NEW.email (use email as initial display name)
     - `created_at` and `updated_at` = NOW()
   - Returns NEW

3. Creates trigger `on_auth_user_created`:
   - AFTER INSERT ON auth.users
   - FOR EACH ROW EXECUTE FUNCTION handle_new_user()

Use standard PostgreSQL syntax compatible with Supabase. Include comments explaining each section.

Apply migration via Supabase CLI:
```bash
cd aruba-leren
npx supabase db push
```

If Supabase CLI not configured, use Supabase Studio SQL editor to run migration manually.
  </action>
  <verify>
Run query in Supabase Studio SQL editor:
```sql
SELECT column_name, data_type FROM information_schema.columns
WHERE table_name = 'profiles' AND column_name IN ('consent_given', 'consent_date');
```
Should return 2 rows. Verify trigger exists:
```sql
SELECT trigger_name FROM information_schema.triggers WHERE trigger_name = 'on_auth_user_created';
```
Should return 1 row.
  </verify>
  <done>
- profiles table has consent_given (boolean) and consent_date (timestamptz) columns
- Trigger on_auth_user_created exists on auth.users table
- Migration file committed to git
  </done>
</task>

<task type="auto">
  <name>Task 2: Create auth server actions</name>
  <files>aruba-leren/src/lib/auth/actions.ts</files>
  <action>
Create `aruba-leren/src/lib/auth/actions.ts` with server actions:

1. **signUp(formData: FormData)**
   - Mark with 'use server'
   - Extract email, password, consentGiven from formData
   - Validate: email format, password min 8 chars, consentGiven === 'true'
   - If consent not given, return { error: 'Toestemming is verplicht' }
   - Create server client via `createClient()` from `@/lib/supabase/server`
   - Call `supabase.auth.signUp({ email, password })`
   - If successful AND user ID exists:
     - Update profiles table: SET consent_given = true, consent_date = NOW() WHERE id = user.id
   - Return { error } or { success: true }

2. **signIn(formData: FormData)**
   - Mark with 'use server'
   - Extract email, password from formData
   - Create server client
   - Call `supabase.auth.signInWithPassword({ email, password })`
   - Return { error } or { success: true }

3. **signOut()**
   - Mark with 'use server'
   - Create server client
   - Call `supabase.auth.signOut()`
   - Return void

Error handling: Translate Supabase error messages to Dutch:
- "Invalid login credentials" → "E-mailadres of wachtwoord is onjuist"
- "Email not confirmed" → "E-mailadres nog niet bevestigd"
- "User already registered" → "E-mailadres is al geregistreerd"

Import createClient from `@/lib/supabase/server` (not client.ts - server actions require server client).

Use redirect() from 'next/navigation' to redirect after successful signIn/signUp:
- After signIn: redirect to `/${locale}/dashboard`
- After signUp: redirect to `/${locale}/dashboard`
- After signOut: redirect to `/${locale}`

Locale detection: extract from formData or use default 'nl' if not provided.
  </action>
  <verify>
Build succeeds: `npm run build` in aruba-leren directory

Check file exists and exports correct functions:
```bash
grep -E "export (async )?function (signUp|signIn|signOut)" aruba-leren/src/lib/auth/actions.ts
```
Should find 3 export statements.
  </verify>
  <done>
- aruba-leren/src/lib/auth/actions.ts exists with signUp, signIn, signOut functions
- All functions marked 'use server'
- signUp updates profiles.consent_given and consent_date
- Build passes without TypeScript errors
  </done>
</task>

</tasks>

<verification>
1. Database schema includes consent fields and auth trigger
2. Server actions file compiles and exports 3 functions
3. No TypeScript errors in build output
</verification>

<success_criteria>
- profiles table extended with consent_given and consent_date columns
- Auth trigger automatically creates profile on user signup
- Server actions (signUp, signIn, signOut) implemented and type-safe
- All code committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-family-accounts/02-01-SUMMARY.md`
</output>
