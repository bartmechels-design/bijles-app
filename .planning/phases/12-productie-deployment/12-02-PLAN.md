---
phase: 12-productie-deployment
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false

must_haves:
  truths:
    - "De tabel leerstof_items bestaat in de Supabase productiedatabase"
    - "De tabel school_vacations bestaat in de Supabase productiedatabase"
    - "De storage bucket 'scratchpads' bestaat in Supabase Storage"
    - "De tabellen report_tokens en study_plans bestaan in de Supabase productiedatabase"
    - "Alle 4 migraties zijn zonder foutmelding uitgevoerd"
  artifacts:
    - path: "(Supabase) Table Editor → leerstof_items"
      provides: "Tabel voor leerstof-uploads (zaakvakken)"
    - path: "(Supabase) Table Editor → school_vacations"
      provides: "Tabel voor vakantierooster (admin panel)"
    - path: "(Supabase) Storage → scratchpads bucket"
      provides: "Private storage bucket voor kladblaadje-afbeeldingen"
    - path: "(Supabase) Table Editor → report_tokens"
      provides: "Tabel voor deelbare rapport-links met PIN"
    - path: "(Supabase) Table Editor → study_plans"
      provides: "Tabel voor studieplannen per kind"
  key_links:
    - from: "Migration 011 (scratchpads)"
      to: "storage.buckets tabel"
      via: "INSERT INTO storage.buckets"
      pattern: "ON CONFLICT (id) DO NOTHING — veilig om opnieuw uit te voeren"
    - from: "Migration 012 (report_tokens)"
      to: "children tabel"
      via: "REFERENCES children(id) ON DELETE CASCADE"
      pattern: "children tabel moet bestaan (aangemaakt in eerdere migraties)"
---

<objective>
Voer de 4 pending SQL-migraties uit in de Supabase productiedatabase via de SQL Editor.

Purpose: De v1.0 code verwijst naar tabellen die nog niet in de productiedatabase staan. Zonder deze migraties crashen de leerstof-upload, het kladblaadje, het vakantierooster en de deelbare rapporten.
Output: Alle 4 tabellen/buckets bestaan in de productiedatabase met de juiste RLS-policies.

Parallelisatie: Deze plan loopt parallel met Plan 01 (Vercel import). Je hebt de productie-URL nog niet nodig voor de SQL-migraties.
</objective>

<execution_context>
@C:/Users/Familie Mechels/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Familie Mechels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-productie-deployment/RESEARCH.md
@aruba-leren/supabase/migrations/008_leerstof_tables.sql
@aruba-leren/supabase/migrations/009_vacation_tables.sql
@aruba-leren/supabase/migrations/011_scratchpads.sql
@aruba-leren/supabase/migrations/012_rapport_tables.sql
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Voer migraties 008 en 009 uit in Supabase SQL Editor</name>
  <action>
    1. Ga naar https://supabase.com/dashboard en open het juiste project
    2. Ga naar de linker navigatie → "SQL Editor"
    3. Klik op "New query" (of het + icoon)

    --- MIGRATIE 008: leerstof_items ---
    4. Kopieer de volledige inhoud van:
       aruba-leren/supabase/migrations/008_leerstof_tables.sql
    5. Plak in de SQL Editor en klik "Run" (of Ctrl+Enter)
    6. Controleer: resultaat toont geen rode foutmelding. "Success" of "1 row affected" is goed.

    --- MIGRATIE 009: school_vacations ---
    7. Klik op "New query" om een nieuwe tab te openen
    8. Kopieer de volledige inhoud van:
       aruba-leren/supabase/migrations/009_vacation_tables.sql
    9. Plak in de SQL Editor en klik "Run"
    10. Controleer: geen rode foutmelding.

    MOGELIJKE FOUT bij 009: "function update_updated_at_column() does not exist"
    → Dit betekent dat de basis-migratie (20260213_initial_schema.sql) niet is uitgevoerd.
    → Controleer via Table Editor of de tabel "profiles" bestaat. Als die ontbreekt, moeten eerst de eerdere migraties worden uitgevoerd. In dat geval: stop en meld dit als een blokker.
  </action>
  <verify>
    Ga naar Supabase → Table Editor (linker navigatie):
    - Klik op de lijst van tabellen en zoek "leerstof_items" → tabel bestaat
    - Zoek "school_vacations" → tabel bestaat
    Beide tabellen moeten zichtbaar zijn in het Table Editor overzicht.
  </verify>
  <done>Tabellen leerstof_items en school_vacations bestaan in de productiedatabase.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Voer migraties 011 en 012 uit in Supabase SQL Editor</name>
  <action>
    Ga terug naar Supabase → SQL Editor.

    --- MIGRATIE 011: scratchpads storage bucket ---
    1. Klik op "New query"
    2. Kopieer de volledige inhoud van:
       aruba-leren/supabase/migrations/011_scratchpads.sql
    3. Plak in de SQL Editor en klik "Run"

    MOGELIJKE FOUT: "policy already exists" voor één van de storage-policies.
    → Dit betekent dat de bucket al eerder (gedeeltelijk) aangemaakt was.
    → Oplossing: Ga naar Supabase → Storage → Policies en controleer welke policies al bestaan.
      Voer dan de SQL uit zonder de CREATE POLICY statements die al bestaan (je kunt die regels tijdelijk verwijderen uit de query).
    → De INSERT INTO storage.buckets met ON CONFLICT DO NOTHING is altijd veilig.

    --- MIGRATIE 012: report_tokens + study_plans ---
    4. Klik op "New query"
    5. Kopieer de volledige inhoud van:
       aruba-leren/supabase/migrations/012_rapport_tables.sql
    6. Plak in de SQL Editor en klik "Run"
    7. Controleer: geen rode foutmelding.

    OPMERKING: Migratie 010 (weekly_email_cron) staat NIET in de lijst voor deze fase en wordt NIET uitgevoerd. Die vereist pg_cron + pg_net + een Edge Function en is buiten de scope van Fase 12.
  </action>
  <verify>
    - Ga naar Supabase → Storage: een bucket genaamd "scratchpads" staat in de lijst (private)
    - Ga naar Supabase → Table Editor: "report_tokens" en "study_plans" staan in de tabellijst
  </verify>
  <done>Storage bucket 'scratchpads' bestaat. Tabellen report_tokens en study_plans bestaan in de productiedatabase. Alle 4 migrations zijn succesvol uitgevoerd.</done>
</task>

</tasks>

<verification>
Controleer in de Supabase Dashboard → Table Editor of alle verwachte tabellen aanwezig zijn:
- leerstof_items (aangemaakt door 008)
- school_vacations (aangemaakt door 009)
- report_tokens (aangemaakt door 012)
- study_plans (aangemaakt door 012)

Controleer in Supabase Dashboard → Storage of de bucket 'scratchpads' aanwezig is (aangemaakt door 011).

Totaal: 4 tabellen + 1 storage bucket = alle 4 pending migrations succesvol.
</verification>

<success_criteria>
- 4 tabellen zichtbaar in Supabase Table Editor: leerstof_items, school_vacations, report_tokens, study_plans
- 1 storage bucket zichtbaar in Supabase Storage: scratchpads (private)
- Geen van de SQL-runs eindigde met een rode foutmelding die niet opgelost werd
</success_criteria>

<output>
Na voltooiing, maak `.planning/phases/12-productie-deployment/12-02-SUMMARY.md` aan met:
- Bevestiging welke migraties succesvol zijn uitgevoerd
- Eventuele problemen die zich voordeden en hoe ze zijn opgelost
- Of migratie 011 policy-conflicten had (en hoe opgelost)
</output>
