---
phase: 09-visuele-leerondersteuning
plan: 04
type: execute
wave: 3
depends_on:
  - 09-02
  - 09-03
files_modified:
  - aruba-leren/src/components/tutor/Scratchpad.tsx
  - aruba-leren/src/components/tutor/ChatInterface.tsx
  - aruba-leren/supabase/migrations/010_scratchpads.sql
autonomous: true

must_haves:
  truths:
    - "Kladblaadje verschijnt automatisch bij rekenen-sessies zodra een [BORD] blok aanwezig is"
    - "Kladblaadje verschijnt ook bij page reload mid-sessie als bestaande berichten [BORD] blokken bevatten"
    - "Kladblaadje-header is niet wegklikbaar — herinnering tekst blijft altijd zichtbaar"
    - "Kind kan tekenen met stylus, vinger of muis op het kladblaadje canvas"
    - "Opslaan-knop uploadt de tekening naar Supabase storage als PNG"
    - "Scratchpad afbeeldingen worden opgeslagen in Supabase storage bucket 'scratchpads' met juiste RLS (parent-read); de parent viewing UI wordt geleverd in Phase 11"
  artifacts:
    - path: "aruba-leren/src/components/tutor/Scratchpad.tsx"
      provides: "Canvas tekenpaneel met kleurkiezer, wis-knop, en opslaan-naar-Supabase"
      exports: ["default Scratchpad"]
    - path: "aruba-leren/src/components/tutor/ChatInterface.tsx"
      provides: "Kladblaadje auto-show logica voor rekenen + [KLADBLAADJE] signal detectie + mount-time restore"
      contains: "showScratchpad"
    - path: "aruba-leren/supabase/migrations/010_scratchpads.sql"
      provides: "scratchpads storage bucket + RLS policies"
      contains: "scratchpads"
  key_links:
    - from: "aruba-leren/src/components/tutor/ChatInterface.tsx"
      to: "aruba-leren/src/components/tutor/Scratchpad.tsx"
      via: "showScratchpad state + subject === 'rekenen' && boardContent aanwezig"
      pattern: "showScratchpad"
    - from: "aruba-leren/src/components/tutor/Scratchpad.tsx"
      to: "supabase storage scratchpads bucket"
      via: "canvas.toBlob() → supabase.storage.from('scratchpads').upload()"
      pattern: "scratchpads"
---

<objective>
Voeg een digitaal kladblaadje toe voor rekenen-sessies. Het kladblaadje verschijnt automatisch wanneer Koko een [BORD] blok toont in een rekenen-sessie — ook bij page reload als bestaande berichten [BORD] blokken bevatten. Een niet-wegklikbare herinnering zorgt dat kinderen het altijd gebruiken. Tekeningen worden opgeslagen naar Supabase voor ouders/tutors.

Purpose: Kinderen op tablets tekenen rekenwerk. Zonder kladblaadje werken ze op papier waar ouders en tutors het niet zien. Digitaal kladblaadje integreert het schrijfwerk in de sessie.
Output: Scratchpad.tsx (nieuw), ChatInterface.tsx updates, SQL migratie 010.

NOTE over wave: Dit plan is Wave 3 (niet Wave 2) omdat zowel 09-02 als 09-04 system-prompts.ts aanpassen. Door 09-02 als dependency toe te voegen wordt een write-conflict op KOKO_BASE_PROMPT vermeden. 09-04 Task 2 voegt de kladblaadje-sectie toe NA 09-02 de wiskundige notatie sectie heeft geschreven.

NOTE over parent viewing UI: De RLS policies in 010_scratchpads.sql staan ouders toe om opgeslagen kladblaadje-afbeeldingen te lezen. De daadwerkelijke viewing UI (een component in het ouderportaal dat de afbeeldingen toont per kind) wordt geleverd in Phase 11 (rapportage). Dit is een bewuste scope-afbakening.
</objective>

<execution_context>
@C:/Users/Familie Mechels/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Familie Mechels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@aruba-leren/src/components/tutor/ChatInterface.tsx
@aruba-leren/src/components/tutor/Whiteboard.tsx
@.planning/phases/09-visuele-leerondersteuning/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Maak Scratchpad.tsx component + SQL migratie 010</name>
  <files>
    aruba-leren/src/components/tutor/Scratchpad.tsx
    aruba-leren/supabase/migrations/010_scratchpads.sql
  </files>
  <action>
**Deel 1: SQL Migratie 010**

Maak `aruba-leren/supabase/migrations/010_scratchpads.sql`:

```sql
-- Migration 010: Scratchpad storage bucket + RLS
-- User must run this in Supabase SQL Editor

-- Create scratchpads storage bucket (private)
INSERT INTO storage.buckets (id, name, public, file_size_limit, allowed_mime_types)
VALUES (
  'scratchpads',
  'scratchpads',
  false,  -- private bucket
  5242880,  -- 5MB limit
  ARRAY['image/png']
)
ON CONFLICT (id) DO NOTHING;

-- RLS: Parents can upload to their own child's folder
-- Path format: scratchpads/{childId}/{sessionId}/{timestamp}.png
CREATE POLICY "Parents can upload scratchpads for their children"
ON storage.objects FOR INSERT
TO authenticated
WITH CHECK (
  bucket_id = 'scratchpads'
  AND (
    -- Allow if the child belongs to this parent
    EXISTS (
      SELECT 1 FROM children c
      JOIN profiles p ON p.id = c.parent_id
      WHERE p.user_id = auth.uid()
      AND c.id::text = split_part(name, '/', 1)
    )
  )
);

-- RLS: Parents can read their own children's scratchpads
-- (viewing UI delivered in Phase 11)
CREATE POLICY "Parents can read scratchpads for their children"
ON storage.objects FOR SELECT
TO authenticated
USING (
  bucket_id = 'scratchpads'
  AND (
    EXISTS (
      SELECT 1 FROM children c
      JOIN profiles p ON p.id = c.parent_id
      WHERE p.user_id = auth.uid()
      AND c.id::text = split_part(name, '/', 1)
    )
  )
);

-- RLS: Admins can read all scratchpads
CREATE POLICY "Admins can read all scratchpads"
ON storage.objects FOR SELECT
TO authenticated
USING (
  bucket_id = 'scratchpads'
  AND (auth.jwt() ->> 'app_metadata')::jsonb ->> 'isAdmin' = 'true'
);
```

---

**Deel 2: Scratchpad.tsx**

Maak `aruba-leren/src/components/tutor/Scratchpad.tsx` als 'use client' component.

**Drawing logica** — extraheer uit Whiteboard.tsx (dezelfde PointerEvent-based canvas tekenen):

```typescript
'use client'

import { useRef, useState, useCallback, useEffect } from 'react'
import { createClient } from '@/lib/supabase/client'

const DRAW_COLORS = ['#1E293B', '#DC2626', '#2563EB', '#16A34A', '#F59E0B']
const LINE_WIDTH = 4

interface ScratchpadProps {
  childId: string
  sessionId: string | null
  isVisible: boolean  // controlled by parent — niet wegklikbaar (geen close knop)
}

export default function Scratchpad({ childId, sessionId, isVisible }: ScratchpadProps) {
  const canvasRef = useRef<HTMLCanvasElement>(null)
  const containerRef = useRef<HTMLDivElement>(null)
  const lastPointRef = useRef<{ x: number; y: number } | null>(null)
  const [isDrawing, setIsDrawing] = useState(false)
  const [drawColor, setDrawColor] = useState(DRAW_COLORS[0])
  const [hasContent, setHasContent] = useState(false)
  const [isSaving, setIsSaving] = useState(false)
  const [saveStatus, setSaveStatus] = useState<'idle' | 'saved' | 'error'>('idle')
  const supabase = createClient()

  // Resize canvas to container
  const resizeCanvas = useCallback(() => {
    const container = containerRef.current
    const canvas = canvasRef.current
    if (!container || !canvas) return
    // Save drawing before resize
    const imageData = canvas.getContext('2d')?.getImageData(0, 0, canvas.width, canvas.height)
    canvas.width = container.clientWidth
    canvas.height = container.clientHeight
    // Fill white background
    const ctx = canvas.getContext('2d')
    if (ctx) {
      ctx.fillStyle = '#FFFFFF'
      ctx.fillRect(0, 0, canvas.width, canvas.height)
      // Draw light grid
      ctx.strokeStyle = '#F3F4F6'
      ctx.lineWidth = 0.5
      const gridSize = 28
      for (let x = gridSize; x < canvas.width; x += gridSize) {
        ctx.beginPath(); ctx.moveTo(x, 0); ctx.lineTo(x, canvas.height); ctx.stroke()
      }
      for (let y = gridSize; y < canvas.height; y += gridSize) {
        ctx.beginPath(); ctx.moveTo(0, y); ctx.lineTo(canvas.width, y); ctx.stroke()
      }
      // Restore drawing (best effort after resize)
      if (imageData) ctx.putImageData(imageData, 0, 0)
    }
  }, [])

  useEffect(() => {
    resizeCanvas()
    window.addEventListener('resize', resizeCanvas)
    return () => window.removeEventListener('resize', resizeCanvas)
  }, [resizeCanvas, isVisible])

  const getPos = (e: React.PointerEvent) => {
    const canvas = canvasRef.current
    if (!canvas) return { x: 0, y: 0 }
    const rect = canvas.getBoundingClientRect()
    return { x: e.clientX - rect.left, y: e.clientY - rect.top }
  }

  const handlePointerDown = (e: React.PointerEvent) => {
    setIsDrawing(true)
    lastPointRef.current = getPos(e);
    (e.target as HTMLElement).setPointerCapture(e.pointerId)
  }

  const handlePointerMove = (e: React.PointerEvent) => {
    if (!isDrawing) return
    const ctx = canvasRef.current?.getContext('2d')
    if (!ctx || !lastPointRef.current) return
    const pos = getPos(e)
    ctx.strokeStyle = drawColor
    ctx.lineWidth = LINE_WIDTH
    ctx.lineCap = 'round'
    ctx.lineJoin = 'round'
    ctx.beginPath()
    ctx.moveTo(lastPointRef.current.x, lastPointRef.current.y)
    ctx.lineTo(pos.x, pos.y)
    ctx.stroke()
    lastPointRef.current = pos
    setHasContent(true)
    setSaveStatus('idle')
  }

  const handlePointerUp = () => {
    setIsDrawing(false)
    lastPointRef.current = null
  }

  const clearCanvas = () => {
    const canvas = canvasRef.current
    const ctx = canvas?.getContext('2d')
    if (!ctx || !canvas) return
    ctx.fillStyle = '#FFFFFF'
    ctx.fillRect(0, 0, canvas.width, canvas.height)
    setHasContent(false)
    setSaveStatus('idle')
  }

  const saveToSupabase = async () => {
    const canvas = canvasRef.current
    if (!canvas || !hasContent) return
    setIsSaving(true)
    try {
      const blob = await new Promise<Blob | null>(resolve => canvas.toBlob(resolve, 'image/png'))
      if (!blob) throw new Error('Canvas is leeg')
      const timestamp = Date.now()
      const path = `${childId}/${sessionId || 'no-session'}/${timestamp}.png`
      const { error } = await supabase.storage
        .from('scratchpads')
        .upload(path, blob, { contentType: 'image/png', upsert: false })
      if (error) throw error
      setSaveStatus('saved')
      setTimeout(() => setSaveStatus('idle'), 3000)
    } catch {
      setSaveStatus('error')
      setTimeout(() => setSaveStatus('idle'), 3000)
    } finally {
      setIsSaving(false)
    }
  }

  if (!isVisible) return null

  return (
    <div className="flex flex-col border-t-2 border-amber-300 bg-amber-50">
      {/* Non-dismissible header */}
      <div className="flex items-center gap-2 px-3 py-2 bg-amber-100 border-b border-amber-200">
        <svg className="w-4 h-4 text-amber-600 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24" strokeWidth={2}>
          <path strokeLinecap="round" strokeLinejoin="round" d="M16.862 4.487l1.687-1.688a1.875 1.875 0 112.652 2.652L10.582 16.07a4.5 4.5 0 01-1.897 1.13L6 18l.8-2.685a4.5 4.5 0 011.13-1.897l8.932-8.931zm0 0L19.5 7.125M18 14v4.75A2.25 2.25 0 0115.75 21H5.25A2.25 2.25 0 013 18.75V8.25A2.25 2.25 0 015.25 6H10" />
        </svg>
        <span className="text-sm font-semibold text-amber-800 flex-1">
          Kladblaadje — reken hier je tussenstappen uit!
        </span>
        {/* Color picker */}
        <div className="flex items-center gap-1">
          {DRAW_COLORS.map(color => (
            <button
              key={color}
              onClick={() => setDrawColor(color)}
              className={`w-5 h-5 rounded-full border-2 transition-transform ${drawColor === color ? 'border-amber-600 scale-125' : 'border-transparent'}`}
              style={{ backgroundColor: color }}
              aria-label={`Kleur ${color}`}
            />
          ))}
        </div>
        {/* Wis knop */}
        <button
          onClick={clearCanvas}
          className="text-xs text-gray-500 hover:text-red-500 px-2 py-0.5 rounded transition-colors"
        >
          Wis
        </button>
        {/* Opslaan knop */}
        <button
          onClick={saveToSupabase}
          disabled={!hasContent || isSaving}
          className={`text-xs font-medium px-2 py-0.5 rounded transition-colors disabled:opacity-40 ${
            saveStatus === 'saved' ? 'bg-green-500 text-white' :
            saveStatus === 'error' ? 'bg-red-500 text-white' :
            'bg-amber-500 text-white hover:bg-amber-600'
          }`}
        >
          {isSaving ? '...' : saveStatus === 'saved' ? 'Opgeslagen!' : saveStatus === 'error' ? 'Fout!' : 'Bewaar'}
        </button>
      </div>
      {/* Canvas area */}
      <div
        ref={containerRef}
        className="h-40 relative bg-white"
        style={{ touchAction: 'none' }}
      >
        <canvas
          ref={canvasRef}
          className="absolute inset-0 cursor-crosshair"
          onPointerDown={handlePointerDown}
          onPointerMove={handlePointerMove}
          onPointerUp={handlePointerUp}
          onPointerLeave={handlePointerUp}
        />
      </div>
    </div>
  )
}
```

**OPMERKING over hoogte:** `h-40` (160px) is bewust compact — genoeg voor tussenstappen, maar neemt niet te veel ruimte in op een tablet. Dit is de minimum bruikbare hoogte voor kinderen.
  </action>
  <verify>
```bash
cd "C:/Users/Familie Mechels/Bijles App/aruba-leren" && npx tsc --noEmit 2>&1 | grep -i "Scratchpad" || echo "No TS errors for Scratchpad"
```
Controleer: `010_scratchpads.sql` bestaat in migrations map.
  </verify>
  <done>
Scratchpad.tsx bestaat en compileert zonder TypeScript-fouten. 010_scratchpads.sql bestaat. Canvas tekenen, kleurwisseling, wis, en Supabase upload logica aanwezig.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integreer Scratchpad in ChatInterface — auto-show bij rekenen + [BORD], inclusief page-reload restore</name>
  <files>aruba-leren/src/components/tutor/ChatInterface.tsx</files>
  <action>
Voeg Scratchpad integratie toe aan ChatInterface.tsx.

**Stap 1: Import**
```typescript
import Scratchpad from './Scratchpad';
```

**Stap 2: State**
Voeg toe na de bestaande state declaraties:
```typescript
const [showScratchpad, setShowScratchpad] = useState(false);
```

**Stap 3: Auto-show logica — nieuwe berichten**

Voeg een useEffect toe die het kladblaadje automatisch toont zodra boardContent wordt gezet:
```typescript
// Auto-toon kladblaadje bij rekenen zodra er een [BORD] blok is
useEffect(() => {
  if (subject === 'rekenen' && boardContent) {
    setShowScratchpad(true)
  }
}, [subject, boardContent])
```

LET OP: `showScratchpad` kan NIET door het kind worden uitgeschakeld via een sluitknop. Het kladblaadje is altijd zichtbaar zodra het geactiveerd is. De `isVisible` prop op Scratchpad is controlled door `showScratchpad` state, maar er is geen knop om het te verbergen.

**Stap 4: Mount-time restore — page reload mid-sessie**

Voeg een tweede useEffect toe die bij mount de bestaande berichtengeschiedenis scant op [BORD] blokken. Dit lost het geval op dat een kind de pagina herlaadt midden in een rekenen-sessie: boardContent is nog niet door een nieuw AI-antwoord gezet, maar de berichten bevatten al [BORD] blokken.

```typescript
// Herstel kladblaadje bij page reload — scan bestaande berichten op [BORD] blokken
useEffect(() => {
  if (subject !== 'rekenen') return
  const hasBordInHistory = messages.some(msg =>
    msg.role === 'assistant' && hasBordBlocks(msg.content)
  )
  if (hasBordInHistory) {
    setShowScratchpad(true)
  }
  // eslint-disable-next-line react-hooks/exhaustive-deps
}, []) // Intentionally runs only on mount — messages array is stable at initial render
```

WAAROM: De `hasBordBlocks` functie is al geïmporteerd vanuit ChatMessage.tsx (gebruikt in handleSubmit). Hergebruik dezelfde helper hier. De lege dependency array `[]` is bewust — we willen dit éénmalig bij mount checken op basis van de initieel geladen berichten, niet bij elke berichtenwijziging (dat doen de andere useEffects al).

**Stap 5: Detecteer [BORD] blokken in handleSubmit**

In `handleSubmit`, in het blok waar AI responses verwerkt worden, voeg ook toe:
```typescript
// Activeer kladblaadje bij rekenen + [BORD]
if (subject === 'rekenen' && hasBordBlocks(assistantMessage)) {
  setShowScratchpad(true)
}
```

**Stap 6: Voeg Scratchpad component toe in de JSX**

Zoek in de render de `{/* Input Area */}` sectie. Voeg het Scratchpad DIRECT ERBOVEN toe (na de messages area maar voor het input-veld):

```tsx
{/* Kladblaadje — altijd zichtbaar bij rekenen zodra [BORD] actief */}
<Scratchpad
  childId={childId}
  sessionId={currentSessionId}
  isVisible={showScratchpad}
/>
```

Positie: tussen het messages scroll-gebied en de input-balk, zodat het kladblaadje altijd zichtbaar is terwijl het kind typt.

**Stap 7: Voeg `[KLADBLAADJE]` instructie toe aan systeem prompt (system-prompts.ts)**

OPMERKING: 09-04 heeft 09-02 als dependency, zodat 09-02 eerst system-prompts.ts aanpast (MATH_FORMAT_RULES). Voeg de kladblaadje-sectie DAARNA toe aan KOKO_BASE_PROMPT — na de wiskundige notatie sectie die 09-02 al heeft geschreven.

Voeg aan de `KOKO_BASE_PROMPT` constante een nieuwe sectie toe NA de `# Opdrachten & Werkblad — [OPDRACHT] tag` sectie (en NA de MATH_FORMAT_RULES die door 09-02 zijn toegevoegd):

```
# Kladblaadje bij Rekenen — Altijd aanwezig

Wanneer je een rekenopgave uitlegt of een [BORD] blok toont in een rekenen-sessie, is er automatisch een digitaal kladblaadje beschikbaar voor het kind. Je hoeft dit niet aan te kondigen — het staat er gewoon.

Echter: herinner het kind actief aan het kladblaadje wanneer je een stap-voor-stap uitwerking geeft:
- "Schrijf elke stap op je kladblaadje!"
- "Gebruik je kladblaadje om de tussenstappen uit te rekenen."
- "Probeer het eerst zelf op het kladblaadje, dan kijken we samen."

Dit helpt het kind om actief mee te doen in plaats van passief te kijken.
```

Voeg dit toe aan het EINDE van de KOKO_BASE_PROMPT string, vlak voor de sluitende backtick.
  </action>
  <verify>
```bash
cd "C:/Users/Familie Mechels/Bijles App/aruba-leren" && npm run build 2>&1 | tail -30
```
Build slaagt zonder fouten.
  </verify>
  <done>
Build slaagt. ChatInterface.tsx importeert Scratchpad. showScratchpad state aanwezig. useEffect voor nieuwe berichten (boardContent) aanwezig. Mount-time useEffect scant messages history op hasBordBlocks() en zet showScratchpad bij page reload. Scratchpad component in JSX tussen messages en input. KOKO_BASE_PROMPT bevat kladblaadje herinnering sectie. Geen sluitknop op kladblaadje — niet wegklikbaar.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` slaagt zonder fouten
2. Scratchpad.tsx bestaat met canvas tekenen + Supabase upload
3. 010_scratchpads.sql bestaat met scratchpads bucket + RLS policies
4. ChatInterface.tsx toont Scratchpad automatisch bij rekenen + boardContent (useEffect op boardContent)
5. ChatInterface.tsx heeft mount-time useEffect die messages history scant op hasBordBlocks() voor page-reload restore
6. Scratchpad heeft geen sluitknop (isVisible controlled, niet dismissible)
7. KOKO_BASE_PROMPT instrueert Koko om kladblaadje te herinneren
8. canvas.toBlob() heeft leeg-canvas check (hasContent guard)
</verification>

<success_criteria>
- Scratchpad verschijnt automatisch bij rekenen-sessies zodra [BORD] actief is
- Scratchpad verschijnt ook na page reload als bestaande berichten [BORD] blokken bevatten
- Header "Kladblaadje — reken hier je tussenstappen uit!" altijd zichtbaar, niet wegklikbaar
- Kind kan tekenen met vinger/stylus/muis in 160px hoge canvas
- Bewaar-knop uploadt PNG naar Supabase scratchpads/childId/sessionId/timestamp.png
- RLS: ouder ziet alleen eigen kind's kladblaadje-afbeeldingen (viewing UI volgt in Phase 11)
- Build slaagt zonder TypeScript-fouten
</success_criteria>

<output>
Na voltooiing, maak `.planning/phases/09-visuele-leerondersteuning/09-04-SUMMARY.md` aan.
</output>
