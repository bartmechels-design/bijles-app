---
phase: 09-visuele-leerondersteuning
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-03
files_modified:
  - aruba-leren/src/components/tutor/Whiteboard.tsx
  - aruba-leren/src/components/tutor/ChatMessage.tsx
  - aruba-leren/src/lib/ai/prompts/system-prompts.ts
  - aruba-leren/src/app/[locale]/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Breuken worden NOOIT als '1/4' getoond — altijd als echte wiskundige notatie (gestapelde teller/noemer)"
    - "KaTeX HTML wordt correct gerenderd in Whiteboard-blokken die LaTeX tokens bevatten"
    - "KaTeX rendering faalt nooit hard — throwOnError: false + try/catch vangt alle parse-fouten op"
    - "KaTeX CSS is globaal geladen via layout.tsx (niet in een 'use client' component)"
    - "Het AI-systeem prompt instrueert Koko om \\frac{1}{4} te schrijven, niet 1/4"
  artifacts:
    - path: "aruba-leren/src/components/tutor/Whiteboard.tsx"
      provides: "renderMathLine() functie die LaTeX tokens detecteert en KaTeX HTML genereert"
      contains: "renderMathLine"
    - path: "aruba-leren/src/components/tutor/ChatMessage.tsx"
      provides: "KaTeX rendering in [OPDRACHT] blokken"
      contains: "renderMathLine"
    - path: "aruba-leren/src/lib/ai/prompts/system-prompts.ts"
      provides: "MATH_FORMAT_RULES sectie die \\frac{}{} notatie afdwingt"
      contains: "MATH_FORMAT_RULES"
    - path: "aruba-leren/src/app/[locale]/layout.tsx"
      provides: "katex/dist/katex.min.css import"
      contains: "katex"
  key_links:
    - from: "aruba-leren/src/lib/ai/prompts/system-prompts.ts"
      to: "aruba-leren/src/components/tutor/Whiteboard.tsx"
      via: "AI emits \\frac{}{} → renderMathLine() detecteert en rendert via katex.renderToString()"
      pattern: "\\\\frac"
    - from: "aruba-leren/src/app/[locale]/layout.tsx"
      to: "aruba-leren/src/components/tutor/Whiteboard.tsx"
      via: "katex CSS globaal geladen, KaTeX HTML-klassen gestijld"
      pattern: "katex.min.css"
---

<objective>
Installeer KaTeX en integreer wiskundige notatie in Whiteboard.tsx en ChatMessage.tsx. Werk het systeem prompt bij om Koko te instrueren \frac{1}{4} te schrijven in plaats van 1/4. Breuken, vermenigvuldiging (×), deling (÷) en wortels (√) worden voortaan als echte wiskundige symbolen weergegeven.

Purpose: Kinderen zien nu breuken als "1/4" (gewone tekst). Dit is mathematisch incorrect en verwarrend. KaTeX maakt gestapelde breuken en correcte symbolen mogelijk.
Output: Werkende KaTeX rendering in Whiteboard + ChatMessage; bijgewerkt systeem prompt.
</objective>

<execution_context>
@C:/Users/Familie Mechels/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Familie Mechels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@aruba-leren/src/components/tutor/Whiteboard.tsx
@aruba-leren/src/components/tutor/ChatMessage.tsx
@aruba-leren/src/lib/ai/prompts/system-prompts.ts
@aruba-leren/src/app/[locale]/layout.tsx
@.planning/phases/09-visuele-leerondersteuning/09-01-SUMMARY.md
@.planning/phases/09-visuele-leerondersteuning/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Installeer KaTeX, voeg CSS import toe, maak renderMathLine helper</name>
  <files>
    aruba-leren/src/app/[locale]/layout.tsx
    aruba-leren/src/components/tutor/Whiteboard.tsx
  </files>
  <action>
**Stap 1: Installeer KaTeX**

Voer uit in `aruba-leren/`:
```bash
npm install katex
npm install --save-dev @types/katex
```

**Stap 2: Voeg KaTeX CSS toe aan layout.tsx**

In `aruba-leren/src/app/[locale]/layout.tsx`, voeg toe aan de imports (BOVEN de `../globals.css` import):
```tsx
import 'katex/dist/katex.min.css';
```

WAAROM in layout.tsx en niet in een component: layout.tsx is een Server Component. CSS imports in Server Components worden correct gebundeld. 'use client' componenten kunnen geen externe CSS importeren zonder Next.js te verwarren.

**Stap 3: Maak renderMathLine() helper in Whiteboard.tsx**

Voeg deze import toe bovenaan Whiteboard.tsx (na de bestaande imports):
```tsx
import katex from 'katex';
```

Voeg deze helper functie toe net BOVEN de `// --- Component ---` comment:

```tsx
// --- KaTeX math rendering ---

/** Detecteer of een string LaTeX-tokens bevat */
function containsMath(text: string): boolean {
  return /\\frac|\\times|\\div|\\sqrt|\\cdot|\^{|_{/.test(text)
}

/**
 * Render een tekstregel met KaTeX als die LaTeX-tokens bevat.
 * Geeft een React element terug met dangerouslySetInnerHTML voor KaTeX HTML.
 * Veilig: throwOnError: false + try/catch vangt alle parse-fouten op.
 */
function renderMathLine(text: string, className?: string): React.ReactElement {
  if (!containsMath(text)) {
    return <span className={className}>{text}</span>
  }
  try {
    const html = katex.renderToString(text, {
      throwOnError: false,
      displayMode: false,
      output: 'html',
      trust: false,
    })
    return (
      <span
        className={className}
        dangerouslySetInnerHTML={{ __html: html }}
      />
    )
  } catch {
    // Fallback: toon plain text als KaTeX toch faalt
    return <span className={className}>{text}</span>
  }
}
```

**Stap 4: Gebruik renderMathLine() in de Whiteboard JSX**

Vervang de plain-text inhoud van `block.text` en `sumLine.digits` door `renderMathLine()`:

- In het `label`-blok render gedeelte: vervang `{block.text}` (de span met de tekst) door `{renderMathLine(block.text)}`
- In het `sum-number` render gedeelte: vervang de inner `{sumLine.digits}` span door `{renderMathLine(sumLine.digits, 'text-xl font-bold text-slate-800 flex-1 text-right tracking-[0.15em]')}`
  - LET OP: verwijder dan de className van de omliggende span want die gaat naar renderMathLine
- In het plain-text blok: vervang `{block.text}` in de `<p>` tag door `{renderMathLine(block.text)}`

BELANGRIJK: renderMathLine retourneert een React element — gebruik het als JSX child, niet als string. Verwijder de omliggende plain-text spans/spans op die plekken.
  </action>
  <verify>
```bash
cd "C:/Users/Familie Mechels/Bijles App/aruba-leren" && npm run build 2>&1 | tail -30
```
Controleer: geen TypeScript-fouten. Geen "Cannot find module 'katex'" errors.
  </verify>
  <done>
Build slaagt. katex is in package.json. layout.tsx importeert 'katex/dist/katex.min.css'. Whiteboard.tsx bevat renderMathLine() en gebruikt het in de render.
  </done>
</task>

<task type="auto">
  <name>Task 2: KaTeX in ChatMessage [OPDRACHT] blokken + systeem prompt update</name>
  <files>
    aruba-leren/src/components/tutor/ChatMessage.tsx
    aruba-leren/src/lib/ai/prompts/system-prompts.ts
  </files>
  <action>
**Stap 1: KaTeX rendering in ChatMessage.tsx [OPDRACHT] blokken**

In `ChatMessage.tsx`:

Voeg import toe bovenaan:
```tsx
import katex from 'katex';
```

Voeg dezelfde `containsMath()` en `renderMathLine()` helper toe (kopieer uit Whiteboard.tsx — zelfde implementatie). Zet het boven de `parseSegments()` functie.

In de render van `segment.type === 'opdracht'`:
Vervang de huidige `<div className="whitespace-pre-wrap text-base">{segment.content}</div>`
door een versie die elke regel door renderMathLine haalt:

```tsx
<div className="text-base">
  {segment.content.split('\n').map((line, li) => (
    <div key={li}>
      {renderMathLine(line, 'whitespace-pre-wrap')}
    </div>
  ))}
</div>
```

Dit werkt lijn-voor-lijn zodat KaTeX alleen actief is op regels die LaTeX bevatten.

**Stap 2: Systeem prompt update voor wiskundige notatie**

In `system-prompts.ts`, voeg een nieuwe constante toe DIRECT NA de `KOKO_BASE_PROMPT` constante definitie (voor de `buildSystemPrompt` functie):

```typescript
/**
 * Math formatting rules — injected in the dynamic part of the system prompt
 * for rekenen sessions. Ensures KaTeX-compatible notation.
 */
export const MATH_FORMAT_RULES = `
# Wiskundige Notatie — Verplichte Opmaak

Gebruik ALTIJD LaTeX-notatie voor wiskundige symbolen in [BORD] blokken en bij uitleg.

## Regels

**Breuken**: Gebruik \\frac{teller}{noemer} — NOOIT "1/4" of "1 op 4"
- Goed: \\frac{1}{4}
- Fout: 1/4

**Vermenigvuldiging**: Gebruik \\times — NOOIT "x" of "*"
- Goed: 3 \\times 4
- Fout: 3 x 4 of 3 * 4

**Deling**: Gebruik \\div — NOOIT "/"
- Goed: 12 \\div 4
- Fout: 12 / 4

**Vierkantswortel**: Gebruik \\sqrt{getal}
- Goed: \\sqrt{16}

**Gewone berekeningen** (als stap-voor-stap met getallen): gebruik de normale som-opmaak (STAP:, getallen uitgelijnd) — geen LaTeX nodig voor pure getallen.

LaTeX wordt ALLEEN gebruikt wanneer er breuken, symbolen of speciale notaties zijn.
`;
```

Voeg daarna MATH_FORMAT_RULES toe aan de `buildSystemPrompt` functie, als extra sectie voor rekenen:

In `buildSystemPrompt`, voeg na de `subjectPrompt` variabele een conditie toe:
```typescript
const mathRules = subject === 'rekenen' ? MATH_FORMAT_RULES : '';
```

En voeg `mathRules` toe aan de array in het return statement, na `subjectPrompt` (maar voor `languageContext`):
```typescript
return [
  staticPrompt,
  subjectPrompt,
  ...(mathRules ? [mathRules] : []),
  languageContext,
  // ... rest blijft hetzelfde
].join('\n\n---\n\n');
```

UPDATE OOK het voorbeeld in `KOKO_BASE_PROMPT` voor breuken:
Zoek in KOKO_BASE_PROMPT de regel: `1. 1/4 + 1/4 = ___`
Vervang door: `1. \\frac{1}{4} + \\frac{1}{4} = ___`

En zoek: `2. 1/3 + 1/3 = ___`
Vervang door: `2. \\frac{1}{3} + \\frac{1}{3} = ___`

En: `3. 2/5 + 1/5 = ___`
Vervang door: `3. \\frac{2}{5} + \\frac{1}{5} = ___`

OPMERKING: In TypeScript strings schrijven we `\\frac` (dubbele backslash) om de LaTeX backslash correct te escapen naar de AI.
  </action>
  <verify>
```bash
cd "C:/Users/Familie Mechels/Bijles App/aruba-leren" && npm run build 2>&1 | tail -30
```
Controleer: build slaagt, geen TypeScript-fouten, geen "import cycle" warnings.
  </verify>
  <done>
Build slaagt. ChatMessage.tsx importeert katex en rendert [OPDRACHT] regels via renderMathLine(). system-prompts.ts heeft MATH_FORMAT_RULES constante en injecteert het voor rekenen-sessies. KOKO_BASE_PROMPT voorbeelden gebruiken \\frac notatie.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` slaagt zonder fouten
2. `package.json` bevat `"katex": "..."` in dependencies
3. `layout.tsx` heeft `import 'katex/dist/katex.min.css'`
4. `Whiteboard.tsx` heeft `containsMath()` en `renderMathLine()` en gebruikt ze
5. `ChatMessage.tsx` heeft `renderMathLine()` in [OPDRACHT] rendering
6. `system-prompts.ts` heeft `MATH_FORMAT_RULES` constante en inject het voor rekenen
7. Geen "throwOnError: true" (moet altijd false zijn)
</verification>

<success_criteria>
- KaTeX is geïnstalleerd en CSS geladen via layout.tsx
- Whiteboard-inhoud met \frac{1}{4} toont gestapelde breuk
- [OPDRACHT] blokken met LaTeX tonen correcte wiskundige notaties
- AI wordt geïnstrueerd om \frac{}{} te schrijven, niet 1/4
- Build slaagt zonder fouten of TypeScript-errors
- Geen harde KaTeX parse-fouten — alle gevallen hebben try/catch fallback
</success_criteria>

<output>
Na voltooiing, maak `.planning/phases/09-visuele-leerondersteuning/09-02-SUMMARY.md` aan.
</output>
