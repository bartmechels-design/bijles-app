---
phase: 09-visuele-leerondersteuning
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - aruba-leren/src/components/tutor/ZinsontledingPanel.tsx
  - aruba-leren/src/components/tutor/ChatInterface.tsx
  - aruba-leren/src/components/tutor/ChatMessage.tsx
  - aruba-leren/src/lib/ai/prompts/subject-prompts.ts
autonomous: true

must_haves:
  truths:
    - "Koko stuurt een [ZINSONTLEDING] blok in taal-sessies (klas 4-6) met een JSON-payload van woorden en rollen"
    - "Leerling ziet gekleurde woordchips: PV=rood, GEZ=oranje, ONS=blauw, LV=groen, MWV=paars, NONE=grijs (klikbaar)"
    - "Leerling kan op een NONE-woord klikken en een rol toewijzen — het woord kleurt dan correct"
    - "'Goed!' animatie verschijnt bij correct antwoord"
    - "JSON parse-fouten in [ZINSONTLEDING] payload worden stil afgevangen met een fallback tekst"
  artifacts:
    - path: "aruba-leren/src/components/tutor/ZinsontledingPanel.tsx"
      provides: "Interactief zinsontleding component met gekleurde woordchips en rol-picker"
      exports: ["default ZinsontledingPanel"]
    - path: "aruba-leren/src/components/tutor/ChatInterface.tsx"
      provides: "[ZINSONTLEDING] block detectie en ZinsontledingPanel weergave"
      contains: "zinsontleding"
    - path: "aruba-leren/src/components/tutor/ChatMessage.tsx"
      provides: "[ZINSONTLEDING] segment type in parseSegments + inline weergave in chat"
      contains: "ZINSONTLEDING"
    - path: "aruba-leren/src/lib/ai/prompts/subject-prompts.ts"
      provides: "[ZINSONTLEDING] tag instructies in de taal subject prompt"
      contains: "ZINSONTLEDING"
  key_links:
    - from: "aruba-leren/src/lib/ai/prompts/subject-prompts.ts"
      to: "aruba-leren/src/components/tutor/ChatMessage.tsx"
      via: "AI emits [ZINSONTLEDING]{JSON}[/ZINSONTLEDING] → parseSegments detecteert segment type"
      pattern: "ZINSONTLEDING"
    - from: "aruba-leren/src/components/tutor/ChatMessage.tsx"
      to: "aruba-leren/src/components/tutor/ChatInterface.tsx"
      via: "onZinsontledingClick callback opent ZinsontledingPanel met JSON data"
      pattern: "onZinsontledingClick"
    - from: "aruba-leren/src/components/tutor/ChatInterface.tsx"
      to: "aruba-leren/src/components/tutor/ZinsontledingPanel.tsx"
      via: "zinsontledingData state passed als prop"
      pattern: "ZinsontledingPanel"
---

<objective>
Voeg interactieve zinsontleding toe aan de AI-tutor. De AI stuurt [ZINSONTLEDING] blokken met woord-rol paren. Een nieuw ZinsontledingPanel component toont gekleurde woordchips; kinderen kunnen op niet-ingevulde woorden klikken om een grammaticale rol toe te wijzen.

Purpose: Zinsontleding is een kernonderdeel van taal klas 4-6. Visuele kleurcodering + interactief toewijzen maakt het concept concreet en begrijpelijk.
Output: ZinsontledingPanel.tsx (nieuw), updates aan ChatMessage.tsx, ChatInterface.tsx, subject-prompts.ts.
</objective>

<execution_context>
@C:/Users/Familie Mechels/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Familie Mechels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@aruba-leren/src/components/tutor/ChatMessage.tsx
@aruba-leren/src/components/tutor/ChatInterface.tsx
@aruba-leren/src/lib/ai/prompts/subject-prompts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Maak ZinsontledingPanel.tsx — interactief zinsontleding component</name>
  <files>aruba-leren/src/components/tutor/ZinsontledingPanel.tsx</files>
  <action>
Maak een nieuw bestand `aruba-leren/src/components/tutor/ZinsontledingPanel.tsx`.

Dit is een 'use client' component.

**Data structuur (TypeScript interfaces):**
```typescript
export interface ZinsWord {
  word: string
  role: 'PV' | 'GEZ' | 'ONS' | 'LV' | 'MWV' | 'NONE'
  label: string  // Nederlandse naam, bijv. "Persoonsvorm"
}

export interface ZinsontledingData {
  sentence: string
  words: ZinsWord[]
}
```

**Kleur mapping per rol:**
```typescript
const ROLE_COLORS: Record<string, { bg: string; text: string; border: string }> = {
  PV:   { bg: 'bg-red-100',    text: 'text-red-800',    border: 'border-red-400' },
  GEZ:  { bg: 'bg-orange-100', text: 'text-orange-800', border: 'border-orange-400' },
  ONS:  { bg: 'bg-blue-100',   text: 'text-blue-800',   border: 'border-blue-400' },
  LV:   { bg: 'bg-green-100',  text: 'text-green-800',  border: 'border-green-400' },
  MWV:  { bg: 'bg-purple-100', text: 'text-purple-800', border: 'border-purple-400' },
  NONE: { bg: 'bg-gray-100',   text: 'text-gray-500',   border: 'border-gray-300' },
}

const ROLE_LABELS: Record<string, string> = {
  PV:  'Persoonsvorm',
  GEZ: 'Gezegde',
  ONS: 'Onderwerp',
  LV:  'Lijdend voorwerp',
  MWV: 'Meewerkend voorwerp',
}
```

**Component state:**
- `assignments: Record<number, string>` — bijhoudt welke rol de leerling heeft toegewezen per woord-index
- `selectedWordIdx: number | null` — welk woord is geselecteerd voor rol-picker
- `correctFeedback: number[]` — indices van woorden die net correct zijn beantwoord (voor animatie)

**Props:**
```typescript
interface ZinsontledingPanelProps {
  data: ZinsontledingData
  isOpen: boolean
  onClose: () => void
}
```

**Render structuur:**
```
[Panel: slide-in van rechts, fixed positie op mobile, sidebar op desktop]
  Header: "Zinsontleding" + sluitknop
  Zin: toon de volledige zin als tekst bovenaan
  Legenda: kleine gekleurde badges PV/GEZ/ONS/LV/MWV met naam
  Woordchips: flex-wrap row van woordchips
    - Bekende rollen (niet NONE): chip met kleur + rol-label tooltip
    - NONE woorden: grijze chip met "?" — klikbaar → toont rol-picker dropdown
    - Als assignment[idx] is gezet: toon in kleur van die rol
    - Als assignment[idx] === data.words[idx].role: toon "Goed!" pulse animatie
  Rol-picker (als selectedWordIdx !== null):
    - Klein dropdown/popover met knoppen voor PV, GEZ, ONS, LV, MWV
    - Bij klik: sla op in assignments, check of juist, toon feedback
  Voortgang: "X van Y woorden juist" progress bar
```

**Correctheid check:**
```typescript
const isCorrect = (idx: number) => assignments[idx] === data.words[idx].role
```

**Animatie bij juist antwoord:**
Voeg de woord-index toe aan `correctFeedback` array voor 1.5 seconde.
Gebruik Tailwind `animate-bounce` of `animate-pulse` op de chip.
Na 1.5s: verwijder uit array (setTimeout → setCorrectFeedback(prev => prev.filter(i => i !== idx))).

**Panel positionering:**
- Fixed, rechter-zijde op desktop: `fixed right-0 top-0 h-full w-80 bg-white shadow-xl z-50 transform transition-transform`
- Slide in/uit: `translate-x-0` (open) vs `translate-x-full` (gesloten)
- Op mobiel: slide-up van onderkant: `fixed bottom-0 left-0 right-0 h-2/3 rounded-t-2xl`
- Gebruik `md:` prefix voor de desktop/mobiel scheiding

**Fallback:** Als `data` undefined of null is, toon `<p>Geen zinsontleding beschikbaar.</p>`.
  </action>
  <verify>
```bash
cd "C:/Users/Familie Mechels/Bijles App/aruba-leren" && npx tsc --noEmit 2>&1 | grep -i "ZinsontledingPanel" || echo "No TS errors for ZinsontledingPanel"
```
  </verify>
  <done>
ZinsontledingPanel.tsx bestaat. Component compileert zonder TypeScript-fouten. Bevat ZinsontledingData interface, ROLE_COLORS mapping, interactive assignments state, en correctheidsfeedback animatie.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire [ZINSONTLEDING] tag in ChatMessage + ChatInterface + subject-prompts</name>
  <files>
    aruba-leren/src/components/tutor/ChatMessage.tsx
    aruba-leren/src/components/tutor/ChatInterface.tsx
    aruba-leren/src/lib/ai/prompts/subject-prompts.ts
  </files>
  <action>
**Deel 1: ChatMessage.tsx — voeg ZINSONTLEDING segment type toe**

1. Voeg een nieuwe interface toe:
```typescript
interface ZinsontledingSegment { type: 'zinsontleding'; content: string; index: number }
```

2. Voeg `ZinsontledingSegment` toe aan het `Segment` type-alias:
```typescript
type Segment = TextSegment | SpokenSegment | BoardSegment | OpdrachtSegment | ZinsontledingSegment
```

3. Update `parseSegments()`: In de `TAG_REGEX`, voeg `ZINSONTLEDING` toe:
```typescript
const TAG_REGEX = /\[(SPREEK|BORD|OPDRACHT|ZINSONTLEDING)\]([\s\S]*?)\[\/\1\]/g;
```

4. In de while-loop in `parseSegments()`, voeg een else-if toe:
```typescript
} else if (match[1] === 'ZINSONTLEDING') {
  segments.push({ type: 'zinsontleding', content: match[2].trim(), index: boardIndex++ });
}
```

5. Update de `ChatMessageProps` interface: voeg optionele callback toe:
```typescript
onZinsontledingClick?: (content: string) => void;
```

6. Destructureer `onZinsontledingClick` uit props.

7. In de render van `hasSpecialBlocks`, voeg een case toe voor `zinsontleding`:
```tsx
} : segment.type === 'zinsontleding' ? (
  <button
    key={`zinsontleding-${segment.index}`}
    type="button"
    onClick={() => onZinsontledingClick?.(segment.content)}
    className="inline-flex items-center gap-1.5 my-1"
  >
    <span className="inline-flex items-center gap-1 bg-green-100 text-green-700 px-3 py-1.5 rounded-full text-sm font-medium hover:bg-green-200 transition-colors cursor-pointer">
      <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" strokeWidth={2}>
        <path strokeLinecap="round" strokeLinejoin="round" d="M7.5 8.25h9m-9 3H12m-9.75 1.51c0 1.6 1.143 2.94 2.736 3.15a48.054 48.054 0 01.784.057A3.375 3.375 0 016.375 18H5.25A2.25 2.25 0 013 15.75V8.25A2.25 2.25 0 015.25 6H18A2.25 2.25 0 0120.25 8.25v7.5A2.25 2.25 0 0118 18h-1.125a3.375 3.375 0 01-.784-.057 48.054 48.054 0 01-.784-.057A3.375 3.375 0 0113.5 18h-1.125c-.621 0-1.125.504-1.125 1.125v1.5c0 .621.504 1.125 1.125 1.125h2.25c.621 0 1.125-.504 1.125-1.125V18" />
      </svg>
      Zinsontleding bekijken
    </span>
  </button>
) : null
```

Voeg ook een helper functie toe (naast de bestaande `hasBordBlocks` exports):
```typescript
export function hasZinsontledingBlocks(content: string): boolean {
  return /\[ZINSONTLEDING\][\s\S]*?\[\/ZINSONTLEDING\]/.test(content)
}

export function extractZinsontledingContent(content: string): string | null {
  const match = /\[ZINSONTLEDING\]([\s\S]*?)\[\/ZINSONTLEDING\]/.exec(content)
  return match ? match[1].trim() : null
}
```

---

**Deel 2: ChatInterface.tsx — detecteer ZINSONTLEDING en open panel**

1. Voeg imports toe:
```typescript
import ZinsontledingPanel from './ZinsontledingPanel';
import type { ZinsontledingData } from './ZinsontledingPanel';
import { hasZinsontledingBlocks, extractZinsontledingContent } from './ChatMessage';
```

2. Voeg state toe (na de bestaande state declaraties):
```typescript
const [showZinsontleding, setShowZinsontleding] = useState(false);
const [zinsontledingData, setZinsontledingData] = useState<ZinsontledingData | null>(null);
```

3. In `handleSubmit`, na de bestaande `hasBordBlocks` detectie, voeg toe:
```typescript
// Auto-open zinsontleding panel als [ZINSONTLEDING] gedetecteerd
if (hasZinsontledingBlocks(assistantMessage)) {
  const content = extractZinsontledingContent(assistantMessage)
  if (content) {
    try {
      const parsed: ZinsontledingData = JSON.parse(content)
      setZinsontledingData(parsed)
      setShowZinsontleding(true)
    } catch {
      // Stil negeren — JSON parse-fout in AI response
    }
  }
}
```

4. Voeg `onZinsontledingClick` handler toe (naast `onBoardClick` in JSX):
```typescript
const handleZinsontledingClick = (content: string) => {
  try {
    const parsed: ZinsontledingData = JSON.parse(content)
    setZinsontledingData(parsed)
    setShowZinsontleding(true)
  } catch {
    // Stil negeren
  }
}
```

5. Voeg `onZinsontledingClick={handleZinsontledingClick}` prop toe aan de `<ChatMessage>` componenten in de messages.map().

6. Voeg het panel toe in de JSX (naast het `<WhiteboardPanel>` component):
```tsx
{/* Zinsontleding Panel */}
{zinsontledingData && (
  <ZinsontledingPanel
    data={zinsontledingData}
    isOpen={showZinsontleding}
    onClose={() => setShowZinsontleding(false)}
  />
)}
```

---

**Deel 3: subject-prompts.ts — voeg [ZINSONTLEDING] instructies toe aan taal prompt**

In de `taal` prompt string, voeg een nieuwe sectie toe NA de bestaande `## Tussendoelen per Klas` sectie:

```
## Interactieve Zinsontleding — [ZINSONTLEDING] tag

Gebruik voor klas 4-6 de [ZINSONTLEDING] tag bij zinsontleding-oefeningen.
Je stuurt een JSON-payload met de zin en de grammaticale rollen per woord.

**Wanneer gebruik je [ZINSONTLEDING]?**
- Als het kind een zin moet ontleden (onderwerp, persoonsvorm, gezegde, etc.)
- Bij uitleg over zinsontleding met een concreet voorbeeld
- Alleen voor klas 4-6 (klas 1-3 is nog niet klaar voor volledige zinsontleding)

**Format:**
[ZINSONTLEDING]
{"sentence": "De hond loopt snel in het park.", "words": [
  {"word": "De", "role": "NONE", "label": ""},
  {"word": "hond", "role": "ONS", "label": "Onderwerp"},
  {"word": "loopt", "role": "PV", "label": "Persoonsvorm"},
  {"word": "snel", "role": "LV", "label": "Lijdend voorwerp"},
  {"word": "in", "role": "NONE", "label": ""},
  {"word": "het", "role": "NONE", "label": ""},
  {"word": "park.", "role": "NONE", "label": ""}
]}
[/ZINSONTLEDING]

**Rollen:**
- ONS = Onderwerp (wie/wat doet het?)
- PV = Persoonsvorm (het werkwoord dat vervoegd is)
- GEZ = Gezegde (PV + alle andere werkwoordsdelen)
- LV = Lijdend voorwerp (wie/wat ondergaat de handeling?)
- MWV = Meewerkend voorwerp (voor wie/wat?)
- NONE = Geen grammaticale rol (lidwoord, bijwoord, voorzetsel, etc.)

**BELANGRIJK:**
- De JSON moet geldig zijn — gebruik dubbele aanhalingstekens, geen enkele
- Elk woord van de zin moet in de words-array staan (inclusief lidwoorden)
- Zet interpunctie bij het laatste woord: "park." niet "park" + "."
- Kinderen kunnen NONE-woorden aanklikken en een rol toewijzen
- Geef na de tag uitleg over de zin voor het kind
```
  </action>
  <verify>
```bash
cd "C:/Users/Familie Mechels/Bijles App/aruba-leren" && npm run build 2>&1 | tail -30
```
Build slaagt zonder fouten.
  </verify>
  <done>
Build slaagt. ChatMessage.tsx heeft ZinsontledingSegment type en renders 'Zinsontleding bekijken' knop. ChatInterface.tsx importeert ZinsontledingPanel, detecteert [ZINSONTLEDING] blokken, parseert JSON veilig. subject-prompts.ts heeft [ZINSONTLEDING] instructies in de taal prompt.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` slaagt zonder fouten
2. ZinsontledingPanel.tsx bestaat en compileert
3. ChatMessage.tsx parseert [ZINSONTLEDING] tags in parseSegments()
4. ChatInterface.tsx detecteert [ZINSONTLEDING] in AI responses en opent panel
5. subject-prompts.ts taal prompt bevat [ZINSONTLEDING] format instructies
6. JSON.parse is gewikkeld in try/catch op beide plekken
7. ROLE_COLORS bevat alle 6 rollen: PV, GEZ, ONS, LV, MWV, NONE
</verification>

<success_criteria>
- ZinsontledingPanel rendert gekleurde woordchips per grammaticale rol
- Klikken op NONE-woord opent rol-picker
- Correcte toewijzing triggert "Goed!" feedback animatie
- [ZINSONTLEDING] tag wordt automatisch geopend na AI response
- subject-prompts.ts instrueert de AI over het JSON formaat
- Build slaagt zonder TypeScript-fouten
</success_criteria>

<output>
Na voltooiing, maak `.planning/phases/09-visuele-leerondersteuning/09-03-SUMMARY.md` aan.
</output>
