---
phase: 11-rapportages-pdf-deling
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - aruba-leren/src/lib/rapport/study-plan-generator.ts
  - aruba-leren/src/components/rapport/StudyPlanEditor.tsx
  - aruba-leren/src/app/[locale]/api/rapport/study-plan/route.ts
  - aruba-leren/src/app/[locale]/dashboard/kind/[childId]/rapport/page.tsx
autonomous: true

must_haves:
  truths:
    - "Rapport toont een automatisch gegenereerd weekstudieplan per vak"
    - "Ouder kan het studieplan aanpassen (minuten en dagen wijzigen per vak)"
    - "Ouder kan het aangepaste plan opslaan; opgeslagen plan blijft zichtbaar bij reload"
    - "Vakken met stuck_concept_count >= 2 krijgen 3 sessies/week, normale vakken 2, niet-geassessed 1"
  artifacts:
    - path: "aruba-leren/src/lib/rapport/study-plan-generator.ts"
      provides: "Rule-based studieplan-generator"
      exports: ["generateStudyPlan", "StudyPlanEntry"]
    - path: "aruba-leren/src/components/rapport/StudyPlanEditor.tsx"
      provides: "Bewerkbaar weekraster-UI voor studieplan"
      contains: "'use client'"
    - path: "aruba-leren/src/app/[locale]/api/rapport/study-plan/route.ts"
      provides: "PUT API-route om studieplan op te slaan in study_plans tabel"
      exports: ["PUT"]
  key_links:
    - from: "aruba-leren/src/components/rapport/StudyPlanEditor.tsx"
      to: "aruba-leren/src/app/[locale]/api/rapport/study-plan/route.ts"
      via: "fetch PUT /api/rapport/study-plan"
      pattern: "api/rapport/study-plan"
    - from: "aruba-leren/src/lib/rapport/study-plan-generator.ts"
      to: "aruba-leren/src/lib/rapport/rapport-data.ts"
      via: "SubjectRapport[] input"
      pattern: "SubjectRapport"
    - from: "aruba-leren/src/app/[locale]/dashboard/kind/[childId]/rapport/page.tsx"
      to: "aruba-leren/src/components/rapport/StudyPlanEditor.tsx"
      via: "render StudyPlanEditor met initPlan prop"
      pattern: "StudyPlanEditor"
---

<objective>
Bouw het studieplan-gedeelte van het rapport: rule-based generator, bewerkbare weekraster-UI, en opslaan-API.

Purpose: Ouders ontvangen een concreet weekplan gebaseerd op hoe hun kind presteert per vak. Stuck-vakken krijgen meer aandacht. Ouder kan het plan aanpassen en bevestigen.
Output: Werkend studieplan in het rapport, bewerkbaar en persistent in de study_plans tabel.
</objective>

<execution_context>
@C:/Users/Familie Mechels/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Familie Mechels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-rapportages-pdf-deling/RESEARCH.md
@.planning/phases/11-rapportages-pdf-deling/11-01-SUMMARY.md
@aruba-leren/src/app/[locale]/dashboard/kind/[childId]/rapport/page.tsx
@aruba-leren/src/lib/rapport/rapport-data.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: study-plan-generator + StudyPlanEditor client-component</name>
  <files>
    aruba-leren/src/lib/rapport/study-plan-generator.ts
    aruba-leren/src/components/rapport/StudyPlanEditor.tsx
  </files>
  <action>
    **study-plan-generator.ts** — pure functions, geen DB-calls, geen AI API-call:
    ```typescript
    import type { SubjectRapport } from './rapport-data';

    export interface StudyPlanEntry {
      subject: string;
      day: 'ma' | 'di' | 'wo' | 'do' | 'vr';
      minutes: number;
      completed: boolean;
    }

    const DAYS: StudyPlanEntry['day'][] = ['ma', 'di', 'wo', 'do', 'vr'];

    export function generateStudyPlan(subjects: SubjectRapport[]): StudyPlanEntry[] {
      const plan: StudyPlanEntry[] = [];
      subjects.forEach((s) => {
        // Allocatie-regel: stuck=3 sessies/week, geassessed=2, niet-geassessed=1
        const sessionsPerWeek = s.stuckEpisodes >= 2 ? 3 : s.assessmentCompleted ? 2 : 1;
        const assignedDays = DAYS.slice(0, sessionsPerWeek);
        assignedDays.forEach((day) => {
          plan.push({ subject: s.subject, day, minutes: 20, completed: false });
        });
      });
      return plan;
    }
    ```

    **StudyPlanEditor.tsx** — "use client" interactieve weekraster:
    - Props: `{ childId: string; initPlan: StudyPlanEntry[]; subjectLabels: Record<string, string> }`
    - State: `plan` (kopie van initPlan), `saving` (boolean), `saved` (boolean)
    - Render een weekraster: rijen = Ma/Di/Wo/Do/Vr, kolommen = vakken (alleen vakken die in initPlan voorkomen)
    - Per cel: als de vakcombinatie bestaat in `plan`, toon een invoerveld voor minuten (type="number", min=5, max=60, step=5) en een checkbox "Gedaan" (completed)
    - Als de cel niet in het plan zit: lege cel (geen invoer)
    - Bouton "Plan opslaan" onderaan: POST naar `/[locale]/api/rapport/study-plan` met body `{ childId, plan }`
    - Na succesvol opslaan: toon "Opgeslagen!" feedback (groen, 3 seconden), zet `saved = true`
    - Gebruik de locale uit de URL: gebruik `useParams()` uit `next/navigation` om de locale te lezen voor het API-pad
    - Tailwind-stijlen: sky-blue thema, tabel met border-collapse, stuck-vakken (stuckEpisodes >= 2) krijgen amber-achtergrond in kolomkop
    - Geen externe state-management library — alleen useState en fetch
  </action>
  <verify>
    `cd "c:/Users/Familie Mechels/Bijles App/aruba-leren" && npx tsc --noEmit 2>&1 | head -30`
    Controleer: generateStudyPlan() retourneert correct aantal entries per vak (stuck=3, normaal=2, niet-assessed=1).
    Controleer: StudyPlanEditor rendert een weekraster zonder runtime-fouten in de browser.
  </verify>
  <done>
    study-plan-generator.ts compileert foutloos en bevat generateStudyPlan(). StudyPlanEditor.tsx rendert bewerkbaar weekraster in de browser. TypeScript geeft 0 fouten.
  </done>
</task>

<task type="auto">
  <name>Task 2: study-plan API-route + koppeling aan rapportpagina</name>
  <files>
    aruba-leren/src/app/[locale]/api/rapport/study-plan/route.ts
    aruba-leren/src/app/[locale]/dashboard/kind/[childId]/rapport/page.tsx
  </files>
  <action>
    **study-plan/route.ts** — PUT endpoint:
    - Volg het patroon van bestaande API-routes in src/app/[locale]/api/tutor/
    - Authenticeer de gebruiker via createClient() (cookie-based)
    - Valideer body: `{ childId: string, plan: StudyPlanEntry[] }`
    - Controleer dat het kind toebehoort aan de ingelogde ouder (join children + profiles op user_id)
    - Gebruik upsert naar study_plans tabel: als rij bestaat (check op child_id), update plan_data en updated_at; anders insert
    - Retourneer `{ success: true }` bij succes, `{ error: string }` bij fout
    - Gebruik `createClient` uit `@/lib/supabase/server` (NIET createAdminClient — ouder moet geauthenticeerd zijn)

    **rapport/page.tsx** — voeg StudyPlanEditor toe aan de rapportpagina:
    - Importeer StudyPlanEditor en generateStudyPlan
    - Bepaal `initPlan`: gebruik `rapportData.studyPlan` als die al bestaat (opgeslagen eerder), anders roep `generateStudyPlan(rapportData.subjects)` aan
    - Verwijder de placeholder-sectie voor studieplan (toegevoegd in plan 11-01) en vervang door `<StudyPlanEditor childId={childId} initPlan={initPlan} subjectLabels={SUBJECT_LABELS} />`
    - Definieer `SUBJECT_LABELS: Record<string, string>` map boven de component met alle 6 vakken in het Nederlands
    - Houd de placeholder-sectie voor "Link genereren" staan (wordt ingevuld in plan 11-03)
  </action>
  <verify>
    `cd "c:/Users/Familie Mechels/Bijles App/aruba-leren" && npx tsc --noEmit 2>&1 | head -30`
    Navigeer naar /nl/dashboard/kind/[childId]/rapport in de browser.
    Controleer: StudyPlanEditor is zichtbaar, weekraster is gevuld.
    Pas een waarde aan en klik "Plan opslaan" — controleer: "Opgeslagen!" feedback verschijnt.
    Reload de pagina — controleer: aangepast plan is persistent.
  </verify>
  <done>
    API route PUT /api/rapport/study-plan slaat plan op in study_plans tabel. Na opslaan en reload blijft het aangepaste plan zichtbaar. TypeScript compileert zonder fouten.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` geeft 0 fouten
- generateStudyPlan() verdeelt sessies correct: stuck=3, normaal=2, niet-assessed=1
- StudyPlanEditor weekraster is bewerkbaar (minuten + completed checkbox per cel)
- Opslaan via de knop schrijft naar study_plans tabel (verifieer in Supabase Table Editor)
- Reload toont het opgeslagen plan (niet de gegenereerde defaults)
</verification>

<success_criteria>
Ouder ziet automatisch gegenereerd studieplan in het rapport. Kan het aanpassen en opslaan. Opgeslagen plan overleeft een page reload. TypeScript bouwt zonder fouten.
</success_criteria>

<output>
Na voltooiing, maak `.planning/phases/11-rapportages-pdf-deling/11-02-SUMMARY.md` aan.
</output>
