---
phase: 11-rapportages-pdf-deling
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - aruba-leren/src/lib/rapport/report-token.ts
  - aruba-leren/src/components/rapport/ShareLinkPanel.tsx
  - aruba-leren/src/app/[locale]/api/rapport/generate/route.ts
  - aruba-leren/src/app/[locale]/rapport/[token]/page.tsx
  - aruba-leren/src/app/[locale]/dashboard/kind/[childId]/rapport/page.tsx
autonomous: true

must_haves:
  truths:
    - "Ouder kan een deelbare link genereren voor het rapport van een kind"
    - "Deelbare link is toegankelijk zonder login op /[locale]/rapport/[token]"
    - "Link toont hetzelfde rapport als de geauthenticeerde pagina (snapshot bij generatie)"
    - "Link verloopt na 30 dagen (expires_at kolom)"
    - "Optionele 4-cijferige PIN beschermt de link; zonder correcte PIN geen rapport zichtbaar"
    - "Kopieer-knop kopieert de volledige URL naar klembord"
    - "Huidig token is zichtbaar op de rapportpagina met vervaldatum"
  artifacts:
    - path: "aruba-leren/src/lib/rapport/report-token.ts"
      provides: "createReportToken() + hashPin() hulpfuncties"
      exports: ["createReportToken", "hashPin"]
    - path: "aruba-leren/src/components/rapport/ShareLinkPanel.tsx"
      provides: "Deel-UI met kopieerknop, PIN-invoer, link-weergave"
      contains: "'use client'"
    - path: "aruba-leren/src/app/[locale]/api/rapport/generate/route.ts"
      provides: "POST API-route om report token aan te maken"
      exports: ["POST"]
    - path: "aruba-leren/src/app/[locale]/rapport/[token]/page.tsx"
      provides: "Publieke rapport-route zonder auth, met PIN-gate"
  key_links:
    - from: "aruba-leren/src/components/rapport/ShareLinkPanel.tsx"
      to: "aruba-leren/src/app/[locale]/api/rapport/generate/route.ts"
      via: "fetch POST /api/rapport/generate"
      pattern: "api/rapport/generate"
    - from: "aruba-leren/src/app/[locale]/rapport/[token]/page.tsx"
      to: "aruba-leren/src/lib/rapport/report-token.ts"
      via: "createAdminClient() token lookup + hashPin()"
      pattern: "createAdminClient"
    - from: "aruba-leren/src/app/[locale]/rapport/[token]/page.tsx"
      to: "aruba-leren/src/components/rapport/RapportView.tsx"
      via: "render RapportView met tokenRow.report_data"
      pattern: "RapportView"
---

<objective>
Bouw het deelbare link-systeem: token-aanmak-API, publieke rapport-route met PIN-gate, en ShareLinkPanel voor de geauthenticeerde rapportpagina.

Purpose: Ouders kunnen het rapport delen via een unieke, beveiligde link zonder dat de ontvanger een account hoeft te hebben. Link verloopt automatisch na 30 dagen.
Output: Werkende /rapport/[token] publieke route + ShareLinkPanel op de rapportpagina.
</objective>

<execution_context>
@C:/Users/Familie Mechels/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Familie Mechels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-rapportages-pdf-deling/RESEARCH.md
@.planning/phases/11-rapportages-pdf-deling/11-01-SUMMARY.md
@aruba-leren/src/lib/auth/admin.ts
@aruba-leren/src/lib/rapport/rapport-data.ts
@aruba-leren/src/components/rapport/RapportView.tsx
@aruba-leren/src/app/[locale]/dashboard/kind/[childId]/rapport/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: report-token lib + generate API-route + ShareLinkPanel</name>
  <files>
    aruba-leren/src/lib/rapport/report-token.ts
    aruba-leren/src/app/[locale]/api/rapport/generate/route.ts
    aruba-leren/src/components/rapport/ShareLinkPanel.tsx
  </files>
  <action>
    **report-token.ts** — server-side hulpfuncties:
    ```typescript
    // hashPin(pin: string): Promise<string>
    // — Gebruik crypto.subtle.digest('SHA-256', ...) — ingebouwd in Node.js (geen library)
    // — Retourneer hex-string
    export async function hashPin(pin: string): Promise<string> {
      const data = new TextEncoder().encode(pin);
      const buf = await crypto.subtle.digest('SHA-256', data);
      return Array.from(new Uint8Array(buf)).map(b => b.toString(16).padStart(2,'0')).join('');
    }

    // createReportToken(childId, reportData, locale, pin?, supabase): Promise<{ token: string; expiresAt: string } | null>
    // — Verwijder eerst alle bestaande tokens voor dit kind (one-token-per-child: eenvoudiger UX)
    // — Hash de PIN als opgegeven
    // — Insert in report_tokens met expires_at = NOW() + 30 days
    // — Retourneer { token, expiresAt } of null bij fout
    ```

    **generate/route.ts** — POST endpoint:
    - Authenticeer via createClient() (cookie-based)
    - Valideer body: `{ childId: string; pin?: string; locale: string }`
    - Verifieer eigenaarschap van kind (join children + profiles)
    - Roep buildRapportData(childId, supabase) aan om de snapshot te maken
    - Roep createReportToken(childId, reportData, locale, pin, supabase) aan
    - Retourneer `{ token: string; expiresAt: string; shareUrl: string }` of `{ error: string }`
    - shareUrl: `${process.env.NEXT_PUBLIC_APP_URL}/[locale]/rapport/[token]` — gebruik de locale uit de request body
    - Als NEXT_PUBLIC_APP_URL niet gezet is, gebruik een relatieve URL als fallback

    **ShareLinkPanel.tsx** — "use client" component:
    - Props: `{ childId: string; childName: string; locale: string; existingToken?: { token: string; expiresAt: string } | null }`
    - State: `token` (string|null), `expiresAt` (string|null), `pin` (string), `generating` (boolean), `copied` (boolean)
    - Initialiseer state vanuit existingToken prop als beschikbaar
    - Render:
      1. PIN-invoerveld (optioneel, max 4 cijfers, type="number") met label "Beveilig met code (optioneel)"
      2. Knop "Link genereren" → POST naar `/[locale]/api/rapport/generate`
      3. Als token aanwezig: toon de volledige deelbare URL in een readonly input
      4. Kopieer-knop naast URL: `navigator.clipboard.writeText(shareUrl)` → toon "Gekopieerd!" 2 seconden
      5. Vervaldatum: "Link geldig tot [datum]" in kleine grijze tekst
      6. Placeholder voor WhatsApp-knop: `{/* WhatsApp-knop wordt toegevoegd in plan 11-04 */}`
    - Tailwind: sky-blue voor genereer-knop, groen voor kopieer-knop, amber voor PIN-sectie
    - Controleer PIN-invoer: als pin.length > 0 && pin.length !== 4 → toon foutmelding "Code moet exact 4 cijfers zijn"
  </action>
  <verify>
    `cd "c:/Users/Familie Mechels/Bijles App/aruba-leren" && npx tsc --noEmit 2>&1 | head -30`
    Controleer: report-token.ts, generate/route.ts, ShareLinkPanel.tsx compileren zonder fouten.
    Test handmatig in de browser: klik "Link genereren" op de rapportpagina → inspecteer network tab → verwacht 200 response met token.
  </verify>
  <done>
    TypeScript compileert. "Link genereren" POST endpoint retourneert 200 met token + expiresAt + shareUrl. ShareLinkPanel toont de URL en kopieer-knop na generatie.
  </done>
</task>

<task type="auto">
  <name>Task 2: Publieke rapport-route + rapportpagina koppeling</name>
  <files>
    aruba-leren/src/app/[locale]/rapport/[token]/page.tsx
    aruba-leren/src/app/[locale]/dashboard/kind/[childId]/rapport/page.tsx
  </files>
  <action>
    **rapport/[token]/page.tsx** — publieke Server Component (geen auth vereist):
    - GEBRUIK createAdminClient() uit `@/lib/auth/admin` — NIET createClient() (geen sessie aanwezig, RLS blokkeert)
    - Params: `{ params: Promise<{ locale: string; token: string }>; searchParams: Promise<{ pin?: string }> }`
    - Flow:
      1. `const adminClient = createAdminClient()`
      2. Query report_tokens: `.eq('token', token).gt('expires_at', new Date().toISOString()).single()`
      3. Als null: render "Link verlopen of ongeldig" pagina (simpele centered div met terugknop naar /)
      4. Als pin_hash aanwezig EN geen pin in searchParams: render PinGateForm (inline component of apart bestand)
      5. Als pin_hash aanwezig EN pin gegeven: hash de pin via hashPin() en vergelijk met pin_hash; bij mismatch: render "Onjuiste code" + nieuw formulier
      6. Als OK: render `<RapportView data={tokenRow.report_data as RapportData} locale={locale} readOnly />`
    - PinGateForm (kan inline in dit bestand als klein "use client" component):
      * Formulier met PIN-invoer (type="number", max 4 cijfers)
      * Submit via GET (action={`/${locale}/rapport/${token}`} method="GET") met hidden input `name="pin"` — eenvoudigste aanpak, geen JavaScript vereist
      * Label "Voer de 4-cijferige code in"
    - RapportView accepteert al een `readOnly` prop (toegevoegd in plan 11-01) — in readOnly-modus verberg de ShareLinkPanel en StudyPlanEditor
    - Importeer hashPin uit `@/lib/rapport/report-token`
    - Voeg metadata toe: `export async function generateMetadata` met title "Voortgangsrapport"

    **rapport/page.tsx** (geauthenticeerd) — voeg ShareLinkPanel toe:
    - Fetch bestaand token voor dit kind: `supabase.from('report_tokens').select('token, expires_at').eq('child_id', childId).gt('expires_at', now).maybeSingle()`
    - Verwijder de placeholder-sectie voor "Link genereren" en vervang door:
      `<ShareLinkPanel childId={childId} childName={rapportData.child.first_name} locale={locale} existingToken={existingToken} />`
    - Importeer ShareLinkPanel
  </action>
  <verify>
    `cd "c:/Users/Familie Mechels/Bijles App/aruba-leren" && npx tsc --noEmit 2>&1 | head -30`
    Test publieke route: genereer een link via de rapportpagina, open de gekopieerde URL in een incognitovenster.
    Verwacht: rapport wordt getoond zonder login.
    Test PIN: genereer een link MET een 4-cijferige PIN → open in incognito → verwacht: PIN-formulier → voer correcte code in → rapport zichtbaar.
    Test verlopen link: update expires_at handmatig in Supabase naar een datum in het verleden → open URL → verwacht: "Link verlopen" melding.
  </verify>
  <done>
    Publieke rapport-route toont rapport voor geldige token zonder auth. PIN-gate blokkeert toegang bij onjuiste code. "Link verlopen" melding bij verlopen token. ShareLinkPanel is zichtbaar op de geauthenticeerde rapportpagina. TypeScript compileert zonder fouten.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` geeft 0 fouten
- `npm run build` slaagt
- Gegenereerde link opent zonder login in incognito → rapport zichtbaar
- PIN-gate blokkeert toegang bij verkeerde PIN
- Kopieer-knop werkt (URL staat in klembord)
- Token vervaldatum is zichtbaar ("Link geldig tot [datum]")
- "Link verlopen" melding bij handmatig verlopen token
</verification>

<success_criteria>
Ouder kan een deelbare link genereren. Link is bereikbaar zonder account. PIN-gate werkt. Kopieer-knop werkt. Link verloopt correct. TypeScript bouwt zonder fouten.
</success_criteria>

<output>
Na voltooiing, maak `.planning/phases/11-rapportages-pdf-deling/11-03-SUMMARY.md` aan.
</output>
