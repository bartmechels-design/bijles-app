---
phase: 06-advanced-tutor-features-content-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - aruba-leren/src/lib/ai/prompts/hiaten-prompts.ts
  - aruba-leren/src/lib/ai/prompts/system-prompts.ts
  - aruba-leren/src/lib/ai/prompts/subject-prompts.ts
  - aruba-leren/src/app/[locale]/api/tutor/chat/route.ts
  - aruba-leren/src/components/tutor/HiatenSelector.tsx
  - aruba-leren/src/components/tutor/ChatInterface.tsx
  - aruba-leren/src/app/[locale]/tutor/[childId]/[subject]/page.tsx
autonomous: true
must_haves:
  truths:
    - "Child can select a hiaat topic before starting a session and Koko focuses exclusively on that topic"
    - "The ?hiaat= URL param pre-selects a topic without requiring child interaction"
    - "SUBJECT_PROMPTS entries contain klas 1-6 tussendoelen examples for each subject"
    - "Hiaat prompt is injected AFTER vakOverride so it has absolute highest priority"
  artifacts:
    - path: "aruba-leren/src/lib/ai/prompts/hiaten-prompts.ts"
      provides: "HIAAT_TOPICS constant keyed by Subject"
      min_lines: 40
    - path: "aruba-leren/src/components/tutor/HiatenSelector.tsx"
      provides: "Topic chip UI for pre-session hiaat selection"
      min_lines: 50
      exports: ["HiatenSelector"]
    - path: "aruba-leren/src/lib/ai/prompts/system-prompts.ts"
      provides: "hiatenTopic? param appended after vakOverride in buildSystemPrompt()"
      contains: "hiatenTopic"
    - path: "aruba-leren/src/app/[locale]/api/tutor/chat/route.ts"
      provides: "hiatenTopic forwarded from request body to buildSystemPrompt()"
      contains: "hiatenTopic"
  key_links:
    - from: "aruba-leren/src/components/tutor/ChatInterface.tsx"
      to: "aruba-leren/src/app/[locale]/api/tutor/chat/route.ts"
      via: "POST body includes hiatenTopic field"
      pattern: "hiatenTopic"
    - from: "aruba-leren/src/app/[locale]/api/tutor/chat/route.ts"
      to: "aruba-leren/src/lib/ai/prompts/system-prompts.ts"
      via: "buildSystemPrompt(... hiatenTopic)"
      pattern: "buildSystemPrompt.*hiatenTopic"
    - from: "aruba-leren/src/app/[locale]/tutor/[childId]/[subject]/page.tsx"
      to: "aruba-leren/src/components/tutor/ChatInterface.tsx"
      via: "initialHiaat prop from searchParams.hiaat"
      pattern: "initialHiaat"
---

<objective>
Add hiaten (learning gap) targeting to Koko sessions: a topic selector chip UI lets children pick a specific weak area before starting, and Koko's system prompt is injected with a mandatory hiaat focus appended after the vakOverride (highest priority). Also enriches SUBJECT_PROMPTS with klas 1-6 tussendoelen examples for Arubaanse Kerndoelen alignment.

Purpose: Children with identified gaps (e.g., breuken, ontleden, samenvatten) can get targeted practice instead of general tutoring. Teachers or parents can also link directly via ?hiaat=topic URL param.

Output: New hiaten-prompts.ts, updated system-prompts.ts, updated route.ts, HiatenSelector component, updated ChatInterface and tutor page.
</objective>

<execution_context>
@C:/Users/Familie Mechels/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Familie Mechels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@aruba-leren/src/lib/ai/prompts/system-prompts.ts
@aruba-leren/src/lib/ai/prompts/subject-prompts.ts
@aruba-leren/src/app/[locale]/api/tutor/chat/route.ts
@aruba-leren/src/components/tutor/ChatInterface.tsx
@aruba-leren/src/app/[locale]/tutor/[childId]/[subject]/page.tsx
@aruba-leren/src/types/tutoring.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hiaten-prompts.ts and enrich SUBJECT_PROMPTS with kerndoelen tussendoelen</name>
  <files>
    aruba-leren/src/lib/ai/prompts/hiaten-prompts.ts
    aruba-leren/src/lib/ai/prompts/subject-prompts.ts
  </files>
  <action>
Create `aruba-leren/src/lib/ai/prompts/hiaten-prompts.ts` with a `HIAAT_TOPICS` constant typed as `Record<Subject, Array<{ id: string; label: string; prompt: string }>>`.

For each subject, define the following topics (label in Dutch, id in snake_case, prompt in Dutch instructing Koko exactly what to focus on):

- taal: spellen_lange_klinkers, spellen_dt_regel, zinsontleding (onderwerp/werkwoord), woordsoorten (zelfstandig/bijvoeglijk naamwoord), samenvatten, dictee_oefening
- rekenen: optellen_aftrekken, tafels_van_vermenigvuldiging, breuken_basis, breuken_gelijknamig, deelsommen, kommagetallen, meten_en_gewichten, woordsommen
- begrijpend_lezen: hoofdgedachte_bepalen, samenvatten, vragen_beantwoorden, moeilijke_woorden, tekststructuur, inferenties_maken
- geschiedenis: chronologie_tijdlijn, oorzaak_gevolg, caquetio_aruba, koloniale_periode, aruba_1986_status_aparte, nederland_geschiedenis
- aardrijkskunde: kaart_lezen, kompasrichtingen, aruba_geografie, klimaat_weeraluba, abc_eilanden, werelddelen
- kennis_der_natuur: planten_dieren_aruba, voedselketen, mensenlichaam, ecosystemen, water_en_lucht, milieu_bescherming

Each `prompt` string should be a 2-3 sentence specific instruction, e.g.:
```
"Focus UITSLUITEND op breuken (teller, noemer, vergelijken, optellen van gelijknamige breuken). Stel ALLEEN vragen over breuken. Gebruik Arubaanse context: verdeel pastechi's, deel strand-stukken op, etc."
```

Also update `aruba-leren/src/lib/ai/prompts/subject-prompts.ts`. For each of the 6 subjects, append a `## Tussendoelen per Klas (Arubaanse Kerndoelen)` section at the end of the existing prompt string. Include concrete skill examples:

- taal klas 1-2: Letters/klanken, CVC woorden, korte zinnen lezen. Klas 3-4: dt-regel, open/gesloten lettergreep, alinea's. Klas 5-6: woordsoorten, zinsontleding, formele/informele taal.
- rekenen klas 1-2: Tellen tot 100, optellen/aftrekken tot 20. Klas 3-4: Tafels, getallen tot 1000, breuken (half, kwart). Klas 5-6: Breuken/decimalen, procenten, oppervlakte/inhoud.
- begrijpend_lezen klas 1-2: Korte tekst (5-8 zinnen), letterlijk begrip. Klas 3-4: Kernzin bepalen, 2-3 paragrafen. Klas 5-6: Tekststructuur, inferenties, samenvatten.
- geschiedenis: Klas 1-2: Heden/verleden/toekomst. Klas 3-4: Caquetio, koloniaal Aruba. Klas 5-6: 1986 Status Aparte, tijdlijn wereld.
- aardrijkskunde: Klas 1-2: Aruba kaart, kompas. Klas 3-4: ABC eilanden, klimaattypen. Klas 5-6: Werelddelen, menselijke geografie, handel.
- kennis_der_natuur: Klas 1-2: Planten/dieren herkennen. Klas 3-4: Voedselketen, habitats. Klas 5-6: Ecosystemen, milieuproblemen, menselijk lichaam.
  </action>
  <verify>
    TypeScript: `cd aruba-leren && npx tsc --noEmit 2>&1 | grep -E "(hiaten|subject-prompts)"` — should produce no errors.
    Confirm HIAAT_TOPICS has entries for all 6 subjects: `grep -c "taal\|rekenen\|begrijpend_lezen\|geschiedenis\|aardrijkskunde\|kennis_der_natuur" aruba-leren/src/lib/ai/prompts/hiaten-prompts.ts`
  </verify>
  <done>
    hiaten-prompts.ts exports HIAAT_TOPICS with all 6 subjects and 5+ topics each. subject-prompts.ts has tussendoelen sections appended for all 6 subjects. No TypeScript errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire hiatenTopic through system-prompts.ts, route.ts, and ChatInterface — add HiatenSelector component</name>
  <files>
    aruba-leren/src/lib/ai/prompts/system-prompts.ts
    aruba-leren/src/app/[locale]/api/tutor/chat/route.ts
    aruba-leren/src/components/tutor/HiatenSelector.tsx
    aruba-leren/src/components/tutor/ChatInterface.tsx
    aruba-leren/src/app/[locale]/tutor/[childId]/[subject]/page.tsx
  </files>
  <action>
**system-prompts.ts:** Add `hiatenTopic?: string` as the last parameter of `buildSystemPrompt()`. After the `vakOverride` const, add a new section:

```typescript
const hiatenOverride = hiatenTopic
  ? `\n\n# HIAAT-FOCUS — ABSOLUTE PRIORITEIT\n\n${hiatenTopic}\n\nDit overschrijft alles. Werk UITSLUITEND aan dit specifieke onderwerp totdat het kind het beheerst.`
  : '';
```

Then add `hiatenOverride` at the END of the join array (after `vakOverride`), so it has the highest priority in the prompt. The `buildAssessmentPrompt()` function does NOT need this parameter — hiaten are only for tutoring.

**route.ts:**
- Add `hiatenTopic?: string` to the `ChatRequestBody` interface.
- Destructure `hiatenTopic` from `body`.
- In the `buildSystemPrompt()` call (tutoring branch only), pass `hiatenTopic` as the final argument.
- Import `HIAAT_TOPICS` from `@/lib/ai/prompts/hiaten-prompts` — NOT used directly in route (ChatInterface resolves the prompt string), so just pass through the string value from the request body.

**HiatenSelector.tsx:** Create a `'use client'` component:
- Props: `subject: Subject`, `onSelect: (topic: { id: string; label: string; prompt: string } | null) => void`, `selected: string | null`
- Import `HIAAT_TOPICS` from `@/lib/ai/prompts/hiaten-prompts`
- Render a flex-wrap row of pill/chip buttons, one per topic for the given subject
- Selected chip: `bg-violet-600 text-white ring-2 ring-violet-300`
- Unselected chip: `bg-white border border-violet-300 text-violet-700 hover:bg-violet-50`
- Include a "Geen specifiek onderwerp" clear button when something is selected
- Heading: `<p className="text-sm font-semibold text-gray-600 mb-2">Wil je aan iets specifieks werken?</p>`

**ChatInterface.tsx:**
- Add `initialHiaat?: string` prop to `ChatInterfaceProps` (the topic id string from URL, e.g. "breuken_basis")
- Add state: `const [hiatenTopic, setHiatenTopic] = useState<string | null>(initialHiaat || null)`
- Add state: `const [sessionStarted, setSessionStarted] = useState(false)` — set to `true` on first message submit
- Before the messages area (when `!sessionStarted` and `!existingSessionId`), render `<HiatenSelector>` passing the subject, `selected={hiatenTopic}`, and `onSelect` handler that resolves the topic prompt string
- In the `handleSubmit` function, when building the POST body, add `hiatenTopic` field. The value to send is the PROMPT string (not the id). Look up the full prompt from `HIAAT_TOPICS[subject].find(t => t.id === hiatenTopic)?.prompt || null`
- After successful first submit, set `setSessionStarted(true)` to hide the selector
- If `huiswerkMode` is active (from Plan 04, which may not exist yet — use a local `false` placeholder), skip the HiatenSelector

**tutor page.tsx:**
- Add `searchParams` to the page props: `searchParams: Promise<{ hiaat?: string }>`
- Await and destructure: `const { hiaat } = await searchParams`
- Pass `initialHiaat={hiaat}` to `<ChatInterface>`
  </action>
  <verify>
    `cd aruba-leren && npx tsc --noEmit 2>&1 | grep -v "node_modules"` — zero errors.
    Check HiatenSelector renders: grep for HIAAT_TOPICS import in ChatInterface.tsx.
    Check route passes hiatenTopic: `grep "hiatenTopic" aruba-leren/src/app/\[locale\]/api/tutor/chat/route.ts`
  </verify>
  <done>
    buildSystemPrompt() accepts hiatenTopic? and injects it after vakOverride. ChatRequestBody has hiatenTopic?. HiatenSelector.tsx renders topic chips. ChatInterface shows selector before first message when session not started. Page reads ?hiaat= param and passes as initialHiaat. Zero TypeScript compile errors.
  </done>
</task>

</tasks>

<verification>
1. `cd aruba-leren && npx tsc --noEmit` — zero errors
2. `grep -n "hiatenTopic" aruba-leren/src/lib/ai/prompts/system-prompts.ts` — shows param and injection point
3. `grep -n "hiatenTopic" aruba-leren/src/app/\[locale\]/api/tutor/chat/route.ts` — shows in ChatRequestBody and buildSystemPrompt call
4. `grep -n "HiatenSelector" aruba-leren/src/components/tutor/ChatInterface.tsx` — shows import and usage
5. `grep -n "searchParams" "aruba-leren/src/app/\[locale\]/tutor/\[childId\]/\[subject\]/page.tsx"` — shows hiaat extraction
6. `grep -n "Tussendoelen" aruba-leren/src/lib/ai/prompts/subject-prompts.ts` — appears 6 times (one per subject)
</verification>

<success_criteria>
- Child opens tutor session for Rekenen, sees chip buttons: "Breuken", "Tafels", "Deelsommen", etc.
- Child selects "Breuken" — chip turns violet
- Child sends first message — HiatenSelector disappears, Koko responds focusing on breuken
- Navigating to `/nl/tutor/[childId]/rekenen?hiaat=breuken_basis` pre-selects the breuken chip
- Koko's system prompt contains HIAAT-FOCUS section injected after vakOverride (verify via console.log in dev)
</success_criteria>

<output>
After completion, create `.planning/phases/06-advanced-tutor-features-content-management/06-01-SUMMARY.md`
</output>
