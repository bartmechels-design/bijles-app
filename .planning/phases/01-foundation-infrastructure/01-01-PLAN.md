---
phase: 01-foundation-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - aruba-leren/middleware.ts
  - aruba-leren/next.config.ts
  - aruba-leren/src/app/[locale]/layout.tsx
  - aruba-leren/src/app/[locale]/page.tsx
  - aruba-leren/src/app/layout.tsx
  - aruba-leren/src/app/globals.css
  - aruba-leren/src/components/LanguageSwitcher.tsx
  - aruba-leren/src/components/Header.tsx
autonomous: true

must_haves:
  truths:
    - "Navigating to /nl/ shows Dutch UI, /pap/ shows Papiamento UI, /es/ shows Spanish UI"
    - "Browser without locale in URL gets redirected based on Accept-Language header, fallback to /nl/"
    - "Language switcher is visible in header on every page and switches locale when clicked"
    - "All UI text (labels, buttons, messages) renders from translation JSON, not hardcoded"
  artifacts:
    - path: "aruba-leren/middleware.ts"
      provides: "next-intl locale routing middleware"
      contains: "createMiddleware"
    - path: "aruba-leren/next.config.ts"
      provides: "Next.js config with next-intl plugin"
      contains: "createNextIntlPlugin"
    - path: "aruba-leren/src/app/[locale]/layout.tsx"
      provides: "Locale-aware root layout with NextIntlClientProvider"
      contains: "NextIntlClientProvider"
    - path: "aruba-leren/src/app/[locale]/page.tsx"
      provides: "Home page using translations"
      contains: "useTranslations"
    - path: "aruba-leren/src/components/LanguageSwitcher.tsx"
      provides: "Language switching UI component"
      contains: "routing.locales"
    - path: "aruba-leren/src/components/Header.tsx"
      provides: "Persistent header with language switcher"
      contains: "LanguageSwitcher"
  key_links:
    - from: "aruba-leren/middleware.ts"
      to: "aruba-leren/src/i18n/routing.ts"
      via: "imports routing config for middleware"
      pattern: "import.*routing"
    - from: "aruba-leren/src/app/[locale]/layout.tsx"
      to: "aruba-leren/src/i18n/request.ts"
      via: "getMessages loads translations"
      pattern: "getMessages"
    - from: "aruba-leren/src/components/LanguageSwitcher.tsx"
      to: "aruba-leren/src/i18n/navigation.ts"
      via: "useRouter/usePathname for locale switching"
      pattern: "useRouter|usePathname"
---

<objective>
Wire next-intl locale routing into the existing Next.js project and create the header with language switcher component.

Purpose: The i18n routing infrastructure is the foundation for all user-facing pages. Without middleware-based locale detection and the [locale] layout, no page can render translated content. The language switcher in the header fulfills the locked decision that it must be "altijd zichtbaar in de header/navbar op elke pagina."

Output: Working trilingual routing (NL/PAP/ES) with locale-in-URL, browser detection fallback to Dutch, and a persistent header with language switcher on every page.
</objective>

<execution_context>
@C:/Users/Familie Mechels/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Familie Mechels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-infrastructure/01-RESEARCH.md
@.planning/phases/01-foundation-infrastructure/01-CONTEXT.md
@aruba-leren/src/i18n/routing.ts
@aruba-leren/src/i18n/navigation.ts
@aruba-leren/src/i18n/request.ts
@aruba-leren/src/messages/nl.json
@aruba-leren/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire next-intl middleware, config, and locale layout</name>
  <files>
    aruba-leren/middleware.ts
    aruba-leren/next.config.ts
    aruba-leren/src/app/[locale]/layout.tsx
    aruba-leren/src/app/[locale]/page.tsx
    aruba-leren/src/app/layout.tsx
    aruba-leren/src/app/globals.css
  </files>
  <action>
    1. Create `aruba-leren/middleware.ts` at project root (NOT in src/):
       - Import `createMiddleware` from `next-intl/middleware`
       - Import `routing` from `./src/i18n/routing`
       - Export default `createMiddleware(routing)`
       - Export config with matcher: `['/((?!api|_next|_vercel|.*\\..*).*)']`
       - This handles Accept-Language header detection with fallback to 'nl' (per locked decision)

    2. Update `aruba-leren/next.config.ts`:
       - Import `createNextIntlPlugin` from `next-intl/plugin`
       - Create plugin instance pointing to `./src/i18n/request.ts`
       - Wrap existing nextConfig with the plugin
       - Keep existing config options

    3. Create `aruba-leren/src/app/[locale]/layout.tsx`:
       - Import `NextIntlClientProvider` from `next-intl`
       - Import `getMessages`, `setRequestLocale` from `next-intl/server`
       - Import `notFound` from `next/navigation`
       - Import `routing` from `@/i18n/routing`
       - Add `generateStaticParams` returning routing.locales.map (CRITICAL for static optimization - see Pitfall 8 in research)
       - Accept `params: Promise<{ locale: string }>` (Next.js 16 async params)
       - Await params, validate locale against routing.locales, call notFound() if invalid
       - Call `setRequestLocale(locale)` for static rendering support
       - Call `getMessages()` to load translation JSON
       - Return html with `lang={locale}`, body wrapping children in `NextIntlClientProvider messages={messages}`
       - Import and apply globals.css here
       - Use Geist font from `next/font/google` (already in the project)
       - Set metadata: title "ArubaLeren", description from translations context

    4. Create `aruba-leren/src/app/[locale]/page.tsx`:
       - Import `useTranslations` from `next-intl`
       - Import `setRequestLocale` from `next-intl/server`
       - Accept `params: Promise<{ locale: string }>`, await and call setRequestLocale
       - Use `useTranslations('home')` to render the home page content
       - Display: title (h1), subtitle, CTA button, and 3 feature highlights
       - Use translations from the existing `home` namespace in nl.json/pap.json/es.json
       - Style with Tailwind: centered layout, responsive, clean modern look
       - This is a Server Component (no 'use client')

    5. Simplify `aruba-leren/src/app/layout.tsx` (root layout):
       - This becomes a minimal pass-through since [locale]/layout.tsx handles everything
       - Keep only the html/body wrapper without fonts or providers (those move to [locale]/layout.tsx)
       - Actually: for next-intl with App Router, the root layout at `src/app/layout.tsx` should be minimal -- just `<html><body>{children}</body></html>` or it can be removed if [locale]/layout.tsx provides html/body. Check: since [locale]/layout.tsx provides `<html lang={locale}>`, the root layout should NOT wrap in html/body again. Make root layout return just `{children}` as a React fragment, OR restructure so only [locale]/layout.tsx has html/body.
       - Best approach: root layout returns `{children}` only (no html/body tags), [locale]/layout.tsx provides the full html/body/providers structure.

    6. Update `aruba-leren/src/app/globals.css`:
       - Keep the Tailwind `@import "tailwindcss"` and `@theme inline` block
       - Remove dark mode media query (not needed for MVP, keep it simple)
       - Keep body styling with sans-serif font
  </action>
  <verify>
    Run `cd aruba-leren && npm run build` -- should complete without errors.
    Run `cd aruba-leren && npm run dev` and verify:
    - Visiting localhost:3000 redirects to localhost:3000/nl/
    - localhost:3000/nl/ shows Dutch home page content
    - localhost:3000/pap/ shows Papiamento home page content
    - localhost:3000/es/ shows Spanish home page content
    - localhost:3000/invalid-locale/ shows 404
  </verify>
  <done>
    Next-intl middleware routes requests to correct locale. [locale]/layout.tsx provides NextIntlClientProvider. Home page renders translated content from JSON files. All 3 locales produce distinct, correctly translated output. Build completes without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Header component with LanguageSwitcher</name>
  <files>
    aruba-leren/src/components/LanguageSwitcher.tsx
    aruba-leren/src/components/Header.tsx
    aruba-leren/src/app/[locale]/layout.tsx
  </files>
  <action>
    1. Create `aruba-leren/src/components/LanguageSwitcher.tsx`:
       - Mark as 'use client' (uses hooks)
       - Import `useLocale, useTranslations` from `next-intl`
       - Import `useRouter, usePathname` from `@/i18n/navigation`
       - Import `routing` from `@/i18n/routing`
       - Render a row of buttons/links for each locale in `routing.locales`
       - On click, call `router.replace(pathname, { locale: newLocale })`
       - Highlight current locale (font-bold or different background)
       - Display locale labels from the `languageSwitcher` namespace in translations (e.g., "Nederlands", "Papiamento", "Espanol")
       - Use Tailwind for styling: inline flex with gap, clean buttons
       - Accessible: aria-label on each button, aria-current on active locale

    2. Create `aruba-leren/src/components/Header.tsx`:
       - Can be a Server Component if LanguageSwitcher is the only client part
       - Import `useTranslations` from `next-intl`
       - Import `Link` from `@/i18n/navigation` for locale-aware links
       - Import `LanguageSwitcher`
       - Render a header/nav bar with:
         - App name "ArubaLeren" as link to home (using `nav.home` or `common.appName`)
         - Navigation links: Home, Privacy (using `nav` namespace translations)
         - LanguageSwitcher component on the right side
       - Style with Tailwind: sticky top-0, white background, shadow-sm, flex between logo/nav and language switcher, responsive (hamburger menu NOT needed for MVP -- keep it simple with horizontal nav)
       - Per locked decision: "Taalswitch altijd zichtbaar in de header/navbar op elke pagina"

    3. Update `aruba-leren/src/app/[locale]/layout.tsx`:
       - Import and render `Header` component inside the body, above {children}
       - Add a main wrapper around {children} for proper page layout (e.g., `<main className="min-h-screen">`)
  </action>
  <verify>
    Run `cd aruba-leren && npm run build` -- no errors.
    Run `cd aruba-leren && npm run dev` and verify:
    - Header appears on home page with "ArubaLeren" logo/name, nav links, and language switcher
    - Clicking "Papiamento" in switcher navigates to /pap/ with Papiamento UI
    - Clicking "Espanol" navigates to /es/ with Spanish UI
    - Header remains visible on all pages (home, privacy route when it exists)
    - Language switcher highlights current active locale
  </verify>
  <done>
    Header with LanguageSwitcher renders on every page. Clicking a language button switches the URL locale and re-renders all UI text in the selected language. Active locale is visually indicated. Navigation links use locale-aware routing.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` in aruba-leren/ completes without errors
2. All 3 locale routes (/nl/, /pap/, /es/) render correct translations
3. Root URL (/) redirects to /nl/ (default locale)
4. Language switcher is visible in header and functional
5. No hardcoded text -- all labels come from translation JSON
</verification>

<success_criteria>
- next-intl middleware intercepts requests and routes to correct locale
- [locale]/layout.tsx wraps all pages in NextIntlClientProvider with correct messages
- Home page renders all content from translation files (home namespace)
- LanguageSwitcher allows switching between NL/PAP/ES with URL update
- Header is persistent across all pages with app name, nav, and language switcher
- Build succeeds with static params for all 3 locales
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-01-SUMMARY.md`
</output>
